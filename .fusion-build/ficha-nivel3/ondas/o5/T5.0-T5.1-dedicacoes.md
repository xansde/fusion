# T5.0-T5.1 — Pin do vendor + dedicações padrão (Onda 5)

Lane: `T5.0-T5.1-dedicacoes` | Worktree: `wt-c` | Branch core: `ficha3/o5` | Branch satélite: `ficha3/o5`

## T5.0 — Pin do vendor

O clone em `tools/importer-pf2e/vendor/pf2e` (junction compartilhada,
somente leitura) já estava no commit correto (`98cb84fa48c8de2048c0b62456e4604bd836bd1a`,
2026-08-23), só não registrado em lugar nenhum.

- `tools/importer-pf2e/README.md`: nova seção "Vendor pin" com o commit, data,
  assunto e o comando exato de reprodução (`git checkout <sha>`, sem `--depth 1`).
- `src/extract.mjs`: `PINNED_VENDOR_COMMIT` + `checkVendorPin()`, chamada no
  `main()` — roda `git rev-parse HEAD` no clone local e **avisa** (stderr,
  nunca falha) se divergir do pin. Testado: sem divergência, roda silencioso;
  lógica de mensagem revisada manualmente (mock não fiz, script real sem
  vendor divergente para testar o caminho positivo).

## T5.1 — 167 dedicações padrão de nível 2

`isStandardArchetypeDedication()` em `build-mvp-subset.mjs`: trait
`dedication` presente, `multiclass` ausente, `level === 2`. **Não** exige o
trait `archetype` — um talento do vendor (`Sanguimancer Dedication`) não o
carrega (bug/quirk confirmado no vendor), e exigi-lo teria deixado o total em
166, divergindo do audit (167) fechado em `plano.md`/`tasks.md`.

Verificação: as 167 dedicações-alvo (levantadas direto do vendor por
script) estão TODAS presentes em `feats-core` após a rebuild — 0 faltando.
`feats-core` sobe de 2759 para 2922 documentos (o delta é +163, não +167,
porque 4 já entravam via outros ramos do filtro — não é bug, é overlap).

## Efeitos colaterais corrigidos (rodar o pipeline completo expôs 2 problemas reais)

`build-mvp-subset.mjs` não tem seletor por pack — rodá-lo pra atualizar
`feats-core` regenera TODOS os packs. Escolhi reverter (`git checkout --`)
todo pack que não fosse `feats-core`, pra manter o diff cirúrgico; isso
revelou e evitou herdar um drift não investigado entre código já mergeado
(`transform.mjs`) e packs committed em 2026-09-15 (`Alertness` some,
3 renomeações — **issue #123**, não investigado a fundo por estar fora do
escopo desta lane).

1. **class-features-core perderia 2 docs autorados à mão** (issue #12,
   Divine Font/Healing Font/Harmful Font) a cada regeneração — README já
   avisava disso sem solução. Implementei `injectHandAuthoredClassFeatures`
   (mecanismo de reinjeção idempotente por `_id`) em vez de reconstruir os
   docs à mão de novo. Como reverti `class-features-core` ao HEAD nesta
   lane, o mecanismo fica pronto pra próxima regeneração real do pack.

2. **grant-resolution-validator.test.ts (T0.4) quebrou**: 4 das novas
   dedicações concedem algo que não resolve localmente (Cathartic Mage →
   classfeature nível 4+; Harrower → ritual fora do subconjunto de
   spells-core; Razmiran Priest/Tattooed Historian → item inicial).
   Registradas no allowlist. **Efeito colateral bom**: 4 entradas antigas da
   issue #92 (Munitions Master/Spellshot/Palatine Detective/Battle Harbinger
   Dedication) ficaram obsoletas — são dedicações PADRÃO e já estão entre as
   167 — removidas, **fechando a issue #92**.

3. **grafo-de-feats.test.mjs**: total de arestas ambíguas travado em 118 caiu
   pra 110 (167 nomes novos entram no universo GLOBAL de candidatos e mudam
   o merge de índice `porNomeGlobal`/`porNomeGlobalTodos`). As 3 arestas
   conhecidas testadas (Rallying Anthem, Shield Block x2, Cleave) continuam
   iguais — só o agregado mudou. Assert atualizado pra 110 com o motivo
   documentado no próprio teste.

## Testes

- Importer (`node --test src/__tests__/*.test.mjs`): **309 passam / 16
  falham — EXATAMENTE o baseline pré-existente** (confirmado com
  `git stash` + rerun; a lista de 16 nomes é idêntica). As 16 são: 4 testes
  sf2e (out/sf2e nunca extraído nesta sessão — infra, não relacionado),
  1 subtest de "MVP packs" (cobertura de class-features-core, não tocado),
  2 normalizeClassSystem/normalizeClassFeatureSystem (Magus, mesmo drift da
  issue #123).
- `packs-validation.test.ts` (system-pf2e): 703/703 verdes.
- `grant-resolution-validator.test.ts` (sheets-pf2e): 4/4 verdes (após o
  fix do allowlist).
- `pregen-parity.test.ts` (sheets-pf2e): 22 falhas, **idênticas byte-a-byte
  ao baseline** (diff vazio) — pré-existentes, não relacionadas.
- `prettier --check` nos 4 arquivos `.mjs`/`.md` tocados: verde.

## Commits

Satélite (`xansde/fusion-systems-2e`, branch `ficha3/o5`, pushed):
- `0275b1c` feat(importer): pina vendor/pf2e e importa as 167 dedicacoes padrao (T5.0/T5.1)
- `b3e6a24` fix(importer): fecha issue #92 e cobre os grants proprios das novas dedicacoes

Core (`xansde/fusion`, branch `ficha3/o5`, pushed):
- `79dec1a0` chore(ficha-nivel3): pina o satelite pos T5.0/T5.1 (dedicacoes padrao)
- `554bc317` chore(ficha-nivel3): repina o satelite (fecha #92 nas dedicacoes T5.1)

Nenhum PR aberto (convenção da onda: "PR por onda", não por lane —
deixado para o fechamento da Onda 5).

## Pendências para issue (todas já abertas)

1. **xansde/fusion-systems-2e#121** — pt-BR: traduzir as 167 dedicações
   novas (backlog do pipeline de tradução subiu de ~1003 para 1166
   pendentes em `feats-core`; conteúdo, não mecanismo — fora do escopo
   desta lane).
2. **xansde/fusion-systems-2e#122** — importar os 36 talentos de
   seguimento (nível 4+) dos mesmos arquétipos, quando uma fatia de nível
   4+ existir (decisão 7 do Alexandre já antecipa isso).
3. **xansde/fusion-systems-2e#123** — drift entre `transform.mjs`
   já mergeado e packs committed em 2026-09-15 (`class-features-core` perde
   `Alertness` e renomeia 3 docs numa regeneração completa) — descoberto,
   não investigado a fundo, revertido nesta lane pra não misturar com T5.1.

## Não verificado

- Não rodei o build/typecheck do CORE inteiro (`pnpm build`/`pnpm typecheck`
  no root) — nenhum arquivo `.ts` foi tocado, só `.mjs`/`.md`/dados JSON dos
  packs; os testes que exercitam esses dados (`packs-validation`,
  `grant-resolution-validator`, `pregen-parity`) rodaram e passaram/bateram
  com o baseline.
- Não testei o caminho de `checkVendorPin()` quando o clone REALMENTE
  diverge (só o caminho "sem divergência", que é o real hoje).
