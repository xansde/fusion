# T7.3 — Roteiro tutorial-e2e da criação nível 1-3 (Onda 7)

## Status: CONCLUÍDO

Commit `80472647` na branch `ficha3/o7` (core, `xansde/fusion`), pushado para o remoto.
Nenhum PR aberto por esta lane (o PR é o fecho da onda, não desta tarefa isolada).

## O que foi feito

1. **Skill `tutorial-e2e` copiada para a worktree** (`.claude/skills/tutorial-e2e/`,
   local/gitignorada em `.git/info/exclude` — não versionada, como o resto do repo).
2. **Roteiro novo**: `.claude/skills/tutorial-e2e/roteiros/ficha-nivel3.spec.ts` (local,
   não commitado — é ferramenta de sessão, mesmo padrão dos outros roteiros da skill).
   Cobre:
   - Ligar a variante **Arquétipo Livre** em Configurações → Mundo.
   - Criar o usuário de teste em Configurações → Usuários ("Criar usuário") — o gatilho
     real de "criar personagem" (gate-runbook.md §5, REQ-USR-025).
   - Abrir a ficha pela gaveta de Contatos.
   - Escolher Ancestralidade (Human) → Linhagem → Antecedente (Scholar) → Classe
     (**Bard**, conjurador espontâneo) — nível 1 completo.
   - Conferir a aba Magias no nível 1 (repertório espontâneo, CD de magia).
   - Subir para o nível 2 e escolher o Talento de Arquétipo — **Acrobat Dedication**,
     uma das dedicações padrão importadas na Onda 5, com "REGRA OPCIONAL ATIVA".
   - Subir para o nível 3.
   - Conferir Principal / Ações / Magias no nível 3.
3. **23 prints capturados e OLHADOS um a um** (Iron Law da skill) — HP progredindo
   16→24→32, CA 13→14→15, salvaguardas subindo, CD de magia do Bardo, Acrobat
   Dedication marcada como concedida no bloco NÍVEL 2, aba Ações mostrando ataques e
   categorias de ação. Tudo condiz com o esperado para um Bard humano nível 1-3 com
   Arquétipo Livre.
4. **Relatório final publicado no repo** (pedido explícito do orquestrador: HTML local,
   sem Artifact): `docs/design/ficha-nivel3/onda7/roteiro-e2e-ficha-nivel3.html`
   (2.7 MB, 23 prints embutidos, `prettier --check` verde).

## Achado de produto (não corrigido — a skill não conserta o app)

**O Actor em branco criado junto com um novo usuário (REQ-USR-025) não chega ao
`worldMirror` da sessão do Mestre que fez a criação.** Confirmado direto no
`world.db`: o Actor existe (`ownership` correto, nome = nome do usuário) imediatamente
após o POST `/api/users` responder 201, mas a seção "Na mesa" de Contatos continua
mostrando a contagem antiga até um `reload()` da página. Contornado no roteiro com
`gm.reload()` logo após criar o usuário; o achado foi documentado, não a causa raiz.

**Issue aberta**: https://github.com/xansde/fusion/issues/254 — "Contacts: Actor
recém-criado via 'Criar usuário' não aparece em 'Na mesa' sem reload".

## Verificação

- `pnpm build`: exit 0 (rodado antes e depois da mudança; único arquivo tocado é o
  `.html` de documentação).
- `prettier --check docs/design/ficha-nivel3/onda7/roteiro-e2e-ficha-nivel3.html`: verde.
- Nenhum código de produção alterado por esta lane — não se aplica typecheck/lint/test
  incremental além do build acima.
- Roteiro e2e real: `1 passed (37.4s)`, 0 falhas de seção, servidor Fusion real +
  browser real + `world.db` real inspecionado diretamente para confirmar o achado.

## Notas para quem continuar

- O roteiro (`ficha-nivel3.spec.ts`) não preenche TODOS os slots de nível (ex.:
  Dádivas de Atributo, Musa, Talento de Ancestralidade, Idioma Bônus ficam com o
  slot aberto) — o escopo de T7.3 é provar o FLUXO (ABC + arquétipo + níveis), não
  esgotar cada slot. T7.2 (comparador, já mergeado na Onda 7a) é quem confere
  campo a campo contra o molde.
- Para rodar de novo: reconstruir `.claude/skills/tutorial-e2e/` a partir do repo
  principal (`C:/Users/xansd/pessoal/fusion/.claude/skills/tutorial-e2e`, copiar) e
  `FUSION_E2E_REPO_ROOT=<worktree>` antes de `playwright test ficha-nivel3.spec.ts`.
