# Onda 4 — T4.3-necromancer — relatório

## Escopo da tarefa

`tasks.md` condiciona T4.3 explicitamente: "Documentos-opção do Fatal Method
(Necromancer), **se a T1.5 confirmar ausência**" (dep `⇠ T1.5`).

## Verificação (não implementação — a condição não se cumpriu)

O relatório da Onda 1 (`ficha3-reports/o1/T1-escolhas.md`, seção T1.6) já
havia concluído que a nota de T1.5 ("sonda não achou os documentos") estava
**desatualizada**: os documentos existem e já estavam tagueados. Reconferi
na worktree desta onda (`wt-c`, branch `ficha3/o4`, após merge de T4.1/T4.2)
em vez de apenas confiar no relatório anterior:

- `external/fusion-systems-2e/systems/pf2e/packs/class-features-core/documents.json`:
  2 documentos com `system.traits.otherTags` contendo `necromancer-fatal-method`
  — **Puppeteer** (`sourceId vt0idZAV9EDD1UN1`) e **Reaper**
  (`sourceId u9U4ySRgjX77gwsq`) — mais o próprio feature-pai "Fatal Method"
  (sem tag, é o placeholder de escolha) e "Grim Fascination" (2º eixo,
  já cabeado na Onda 1 também).
- Consumo no código já existe: `grep` por `fatalMethod` acha
  `sheets/pf2e/src/lib/sheets/pf2e/planVM.ts`,
  `sheets/pf2e/src/lib/sheets/pf2e/choiceSetInventory.ts` (picker real,
  ligado a `CLASS_CHOICE_SLOT_OPTIONS`) e o teste
  `__tests__/ficha-nivel3-onda1.test.ts` (casos dedicados de Necromancer,
  ambos eixos — fatalMethod e grimFascination — dentro dos 863 testes verdes
  já reportados pela Onda 1).

Ou seja: a pré-condição de T4.3 ("se a T1.5 confirmar ausência") **não se
confirmou** — o oposto aconteceu, a T1.6 já havia fechado o caso na Onda 1.
Não há dado ausente para importar/curar, não há picker para ligar. Nenhum
código foi alterado nesta lane.

## Prova mecânica

Nenhum arquivo tocado — verificação, não implementação. Não há linha nova
na tabela da onda além da confirmação do estado herdado da Onda 1.
`pnpm build`/testes não foram re-executados (nada mudou); os 863 testes de
superfície + 660 do system-pf2e já cobrem `fatalMethod` desde o merge de
`ficha3/o1`.

## Commits

Nenhum. Nada para commitar/pushar nesta lane (branch `ficha3/o4` do satélite
e do core seguem só com os commits de T4.1/T4.2, já presentes na worktree).

## Pendências (issue)

Nenhuma pendência nova. Nada a registrar.

## Resumo (contrato de retorno)

Status: **OK**. T4.3 era condicional a T1.5 confirmar ausência dos
documentos-opção do Fatal Method (Necromancer); reconferi na worktree
`wt-c` e a condição não se cumpriu — a T1.6 (Onda 1) já havia confirmado
que os 2 documentos (Puppeteer, Reaper) existem, estão tagueados
(`necromancer-fatal-method`) e já estão cabeados em `planVM.ts` +
`choiceSetInventory.ts`, cobertos por `ficha-nivel3-onda1.test.ts`. Nenhum
código alterado, nenhum commit, nenhuma pendência nova. Onda 4 segue com
T4.1 e T4.2 como únicas lanes com trabalho de fato.
