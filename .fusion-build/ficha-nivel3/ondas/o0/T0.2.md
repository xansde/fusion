# T0.2 — Guard KNOWN_CLASS_TRAITS (Onda 0)

Worktree: `.../scratchpad/wt-o0`. Satélite `external/fusion-systems-2e`, branch `ficha3/onda0`,
partindo do tip `073fb5e`.

## O que fez

1. **Baseline** (`baseline.md`, no mesmo diretório): `pnpm typecheck` exit 0; `pnpm test`
   (workspace inteiro) exit 1 com 25 testes/3 arquivos falhando — todos dívidas já documentadas
   (`pregen-parity.test.ts` 22, `actionCategories.test.ts` 1, `traitNames.sync.test.ts` 2), nada
   relacionado a `isFeatEligible`/`KNOWN_CLASS_TRAITS`. Também 1 "Timeout calling onTaskUpdate"
   (flakiness de infra conhecida do vitest sob carga, não teste vermelho).

2. **PROVEI o contexto do orquestrador** (não confiei): inspecionei `planVM.ts` no tip
   `073fb5e` — `KNOWN_CLASS_TRAITS` já tem as **29** classes (`magus, alchemist, animist,
   barbarian, bard, champion, cleric, commander, druid, exemplar, fighter, guardian, gunslinger,
   inventor, investigator, kineticist, monk, necromancer, oracle, psychic, ranger, rogue,
   runesmith, sorcerer, summoner, swashbuckler, thaumaturge, witch, wizard`), batendo 1:1 com os
   29 nomes de classe em `classes-core/documents.json` (lowercased). O commit `9795545` na
   branch já cobre as 29, não só as 27 originais. **O buraco 3 do `plano.md` está desatualizado**
   — o guard já existia; o que faltava era só cobertura de teste.

3. **Escrevi o teste** (não existia nenhum exercitando `isFeatEligible`/`KNOWN_CLASS_TRAITS` com
   dado real de pack): `sheets/pf2e/src/lib/sheets/pf2e/__tests__/classFeatLeakGuard.test.ts`, 5
   casos, não-circular (carrega `classes-core`/`feats-core` reais do disco e roda a função real,
   nunca recomputa "esperado" a partir do próprio `KNOWN_CLASS_TRAITS`):
   - (a) todo slug de `classes-core` está em `KNOWN_CLASS_TRAITS` — classe nova sem guard quebra
     aqui.
   - (b) para as 29 classes, nenhum classFeat nível 1-3 de OUTRA classe (traço único) é elegível
     no picker — 116 combinações checadas por classe × feats de dono único (>50 feats de dono
     único no total).
   - (c) sanity — o próprio feat de dono único é elegível para o picker da própria classe (não
     vacuamente falso).
   - (d) exceção de multi-classe: feats com 2+ traços de classe conhecidos (`Familiar`:
     magus/sorcerer/thaumaturge/wizard; `Reach Spell`: bard/cleric/druid/oracle/sorcerer/
     witch/wizard etc., 116 feats no total) continuam elegíveis para CADA classe listada.
   - (e) mutação direta: com "psychic" fora do guard, o mesmo feat real (`Ancestral Mind` ou
     similar) deixa de ser reconhecido como class-tagged.
   - **Fronteira arquétipo × trait de classe**: verificado por varredura (0 feats com
     `category:class` + trait `archetype` carregando também um trait de classe conhecido) — sem
     ambiguidade a documentar nesta lane; a exclusão de `archetype` no filtro do teste espelha
     `isFeatEligible`'s próprio branch `classFeat` (que já rejeita `archetype` antes de checar o
     `classSlug`).

4. **Mutação real**: comentei `"psychic"` do `Set` em `planVM.ts`, rodei o teste — 3 dos 5 casos
   ficaram vermelhos pelo motivo certo (leak de `Ancestral Mind`/`Mental Buffer`/etc. para TODAS
   as outras 28 classes, listado nome a nome no diff da asserção). `git restore` no arquivo,
   reaplicado só o `export` (ver item 5) — confirmei com `grep` que "psychic" voltou ao Set.

5. **Código tocado**: `KNOWN_CLASS_TRAITS` era módulo-privado (`const`); exportei
   (`export const KNOWN_CLASS_TRAITS`) para o teste ler a fonte da verdade direto, em vez de
   hardcodar as 29 classes no teste (o que reintroduziria o mesmo risco que o teste existe para
   pegar). Nenhuma outra mudança de código — o guard em si já cobria as 29.

## Gates (todos verdes, saída completa nos logs — não colada aqui)

| Gate | Log | Resultado |
|---|---|---|
| `pnpm build` (topológico, root) | `build.log` | exit 0 |
| `pnpm typecheck` (root) | `typecheck.log` | exit 0 |
| `pnpm lint` (root) | `lint.log` | exit 0 |
| `pnpm --filter @fusion/sheets-pf2e exec vitest run` | `test-sheets-pf2e.log` | 2 arquivos/23 testes falharam — **idênticos ao baseline** (`pregen-parity.test.ts` 22, `actionCategories.test.ts` 1); `classFeatLeakGuard.test.ts` nos 23 passados. **Zero regressão.** |
| `prettier --check` nos 2 arquivos tocados | inline | "All matched files use Prettier code style!" |

## Commits

- `bb167c1` — `test(sheets-pf2e): confirma o guard KNOWN_CLASS_TRAITS para as 29 classes (T0.2)`
  em `ficha3/onda0` do satélite (`xansde/fusion-systems-2e`).
- Push: `git push -u origin ficha3/onda0` (após `gh auth switch -u xansde`) — branch nova no
  remoto, sem PR aberto ainda (a lane não pediu abertura de PR; fica para o fecho da Onda 0).

## Decisões e porquês

- **Não editei `KNOWN_CLASS_TRAITS` além do `export`** — o guard já cobria as 29 classes; inventar
  mudança onde não fazia falta violaria a instrução "se já existir cobrindo tudo, não invente
  mudança".
- **Teste deriva os slugs de classe do pack, não de uma lista hardcoded de 29** — assim, se uma
  30ª classe for adicionada depois sem entrar no guard, o teste (a) e (b) quebram sem precisar
  tocar neste arquivo de novo (propriedade estrutural, não uma foto do estado atual).
- **Arquivo de teste novo, não emenda em `planVM.test.ts`** — `planVM.test.ts` já tem ~3600
  linhas; o guard é uma preocupação isolada (dado real de pack vs. lógica pura de VM testada com
  fixtures sintéticas no resto do arquivo).

## Pendências para issue

Nenhuma pendência nova gerada por esta lane. Achado relevante para quem fechar a Onda 0 (não é
issue, é correção de rota do plano): **`docs/design/ficha-nivel3/plano.md`, buraco 3, está
desatualizado** — diz "o commit que adiciona as classes novas a KNOWN_CLASS_TRAITS não é
ancestral desta branch"; no tip `073fb5e` ele **é** ancestral (commit `9795545`). Sugiro ao
orquestrador marcar o buraco 3 como fechado ao costurar a onda, para não gerar retrabalho na T0.5
ou em ondas futuras.

## Resumo (campo retorno)

Ver campo `resumo` da resposta — este arquivo é o relatório completo.
