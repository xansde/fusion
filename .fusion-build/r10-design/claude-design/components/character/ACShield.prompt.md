ACShield — the Armour Class stat, rendered inside a hexagon-clipped shield. It's the single shaped element in the otherwise-rectangular stat block, so use it only for CA.

```jsx
<ACShield value={19} />
```

Value is mono and large; the "CA" caption sits beneath. Size via `width`/`height` if you need it larger in a header.
