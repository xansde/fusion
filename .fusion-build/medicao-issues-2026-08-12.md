# Medição das issues abertas do Fusion — 2026-08-12

> Medido por 9 agentes contra `build/app` @ `aff170a`, **antes** das ondas A e B.
> Método: nunca julgar pelo texto da issue — refazer a medição que ela cita contra o código/packs de hoje.
> As 17 issues fechadas na triagem não estão aqui: a evidência de cada uma virou comentário na própria issue.

**ATENÇÃO:** os números de linha são de ANTES das ondas A e B. Confira `arquivo:linha` no código atual antes de editar.

| # | Status | Esforço | Área |
|---|---|---|---|
| #3 | NUMERO_DESATUALIZADO | G | Onda C — packs: pré-requisitos e nomes (#3, #16, #26, #28, #30, #44, #45, #46) |
| #5 | CONFIRMADO | G | Onda A — Conjuração no planVM (issues #5, #13, #7, #8, #34) |
| #6 | CONFIRMADO | P | Runtime de foco e Bardo (ficha) — issues #6, #36, #37, #50 |
| #7 | CONFIRMADO | M | Onda A — Conjuração no planVM (issues #5, #13, #7, #8, #34) |
| #8 | CONFIRMADO | M | Onda A — Conjuração no planVM (issues #5, #13, #7, #8, #34) |
| #9 | CONFIRMADO | P | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #11 | CONFIRMADO | P | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #12 | CONFIRMADO | P | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #13 | CONFIRMADO | M | Onda A — Conjuração no planVM (issues #5, #13, #7, #8, #34) |
| #18 | CONFIRMADO | G | Avaliador de pré-requisitos / picker de talentos (planVM.ts, PF2e client) — lote de confirmação pós-merge #111/#112 |
| #20 | CONFIRMADO | M | Avaliador de pré-requisitos / picker de talentos (planVM.ts, PF2e client) — lote de confirmação pós-merge #111/#112 |
| #26 | NUMERO_DESATUALIZADO | P | Onda C — packs: pré-requisitos e nomes (#3, #16, #26, #28, #30, #44, #45, #46) |
| #28 | NUMERO_DESATUALIZADO | M | Onda C — packs: pré-requisitos e nomes (#3, #16, #26, #28, #30, #44, #45, #46) |
| #29 | CONFIRMADO | M | Avaliador de pré-requisitos / picker de talentos (planVM.ts, PF2e client) — lote de confirmação pós-merge #111/#112 |
| #30 | CONFIRMADO | M | Onda C — packs: pré-requisitos e nomes (#3, #16, #26, #28, #30, #44, #45, #46) |
| #33 | CONFIRMADO | M | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #34 | CONFIRMADO | G | Onda A — Conjuração no planVM (issues #5, #13, #7, #8, #34) |
| #36 | CONFIRMADO | M | Runtime de foco e Bardo (ficha) — issues #6, #36, #37, #50 |
| #37 | CONFIRMADO | G | Runtime de foco e Bardo (ficha) — issues #6, #36, #37, #50 |
| #38 | CONFIRMADO | M | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #39 | CONFIRMADO | P | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #42 | CONFIRMADO | M | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #43 | CONFIRMADO | M | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #46 | NUMERO_DESATUALIZADO | P | Onda C — packs: pré-requisitos e nomes (#3, #16, #26, #28, #30, #44, #45, #46) |
| #49 | CONFIRMADO | M | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #50 | CONFIRMADO | G | Runtime de foco e Bardo (ficha) — issues #6, #36, #37, #50 |
| #51 | NUMERO_DESATUALIZADO | G | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #52 | CONFIRMADO | G | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #53 | CONFIRMADO | G | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #54 | CONFIRMADO | M | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #55 | CONFIRMADO | G | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #56 | NUMERO_DESATUALIZADO | G | Onda C — packs: tradução e regras inertes (issues #32, #51, #52, #53, #54, #55, #56, #47, #33, #9, #40, #42, #43) |
| #59 | NUMERO_DESATUALIZADO | G | Onda B — classe e ficha (issues #12, #38, #39, #49, #11, #58, #59) |
| #62 | CONFIRMADO | P | Onda D — ficha em jogo: acoes, pericias, descanso (issues #62, #63, #64, #65, #72) |
| #81 | CONFIRMADO | M | Onda D — token, parede e protocolo do servidor (#83, #81, #84) |
| #83 | CONFIRMADO | G | Onda D — token, parede e protocolo do servidor (#83, #81, #84) |
| #84 | CONFIRMADO | P | Onda D — token, parede e protocolo do servidor (#83, #81, #84) |

---

## #3 — NUMERO_DESATUALIZADO (esforço G)

**Medição:** node -e sobre feats-core+class-features-core, mesmo método da issue: hoje 254 regras kind:'grant-item' (era 137 na issue, 272 na triagem de 12/08 — packs seguem sendo regerados), 0 apontam pra spells-core (/spells/.test(uuid)) — problema central INTACTO. Ampliei a evidência: 0/18 docs 'Bloodline: <Nome>' em class-features-core têm grant-item (a magia de sangue nunca é concedida); 167 docs de feats-core têm system.rules:[] E uma @UUID[...spells-srd...] na descrição (a issue citava 38 com critério mais estrito de nome-igual-ao-talento). O overlay mechanics.json (extrator de prosa 'Conflux Spell') tem hoje 8 fixed-item apontando pra spells-srd (era 7) — mas confirmei que os 8 são EXCLUSIVAMENTE as 8 Hybrid Studies do Magus (Twisting Tree, Laughing Shadow, Resurgent Maelstrom, Sparkling Targe, Starlit Span, Aloof Firmament, Inexorable Iron, Unfurling Brocade) — nenhuma classe nova ganhou cobertura.

**Arquivos:** `tools/translate-packs/src/conflux-grants.mjs (extrator hoje casado com o rótulo 'Conflux Spell', só Magus)`; `tools/translate-packs/src/grants-from-rules.mjs (orquestra os extratores; precisa de um novo extrator por classe/padrão de prosa)`; `packages/client/src/lib/sheets/pf2e/grantMaterializer.ts:282-284 (mapVendorToFusionPack já mapeia spells-srd -> spells-core, nunca exercido fora do Magus)`

**Esboço de correção:** Criar um novo extrator curado (paralelo ao conflux-grants.mjs) para o padrão de prosa da magia de sangue do Feiticeiro ('You gain the @UUID[...] spell' na descrição de cada 'Bloodline: X') e regenerar mechanics.json; repetir para Cleric doctrine/Bard muse quando o padrão de prosa for igualmente inequívoco (não todo caso é fixed-grant — muitos são escolha, e escolha já é outro kind). Teste NÃO-CIRCULAR: para cada uma das 18 'Bloodline: X' em class-features-core, extrair a spell citada no UUID da própria descrição (fonte = prosa do pack, não uma tabela do teste) e afirmar que materializeGrants produz um doc:create para essa spell em spells-core — mesmo padrão do teste de censo do #16 (grantMaterializer.test.ts:1179+), sem reimplementar a lógica de resolução.

---

## #5 — CONFIRMADO (esforço G)

**Medição:** node -e "const c=require('./systems/pf2e/packs/classes-core/documents.json');for(const k of c)console.log(k.name,(k.system.featuresByLevel||[]).filter(f=>f.level===1&&/ Spells$/.test(f.name)).map(f=>f.name))" → só Bard/Champion/Magus/Sorcerer têm feature nível 1 terminada em ' Spells' (4/12 classes, igual ao número da issue). Cleric e Wizard têm system.spellcasting.tradition preenchido (divine/wis, arcane/int) mas hasFocusFeature()=false → ganham só a entry principal, sem foco. Monk e Ranger têm system.spellcasting undefined E hasFocusFeature()=false → NÃO ganham NENHUMA entry (confirmado lendo applyClass, planVM.ts:2994-3011). Contagem de magias sem destino: cleric=138, wizard=39, ranger=22, monk=15 → soma 214/458 (47%), idêntico ao citado na issue.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:3138 (hasFocusFeature — heurística de nome)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:2994-3011 (applyClass — 1º call site)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:4004-4013 (bloodlineSpellcastingOps — 2º call site, Sorcerer)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:4886-4937 (chooseClassLevel — 3º call site, multiclasse: NEM SEQUER tem branch de foco hoje, só chama buildSpellcastingEntryOp na linha 4930)`

**Esboço de correção:** hasFocusFeature cobre só o caso 'a classe já nasce com o foco'. Falta reconhecer foco concedido por um TALENTO ou EIXO escolhido depois (Cleric: feat 'Domain Initiate', Monk: feat 'Qi Spells', Ranger: feat 'Initiate Warden', Wizard: eixo 'Arcane School'). Nenhum desses 4 tem sinal estruturado no pack (Domain Initiate só tem um ChoiceSet NÃO convertido — flags.fusion.unconvertedRules; Qi Spells tem system.rules=[] vazio; Arcane School tem um GrantItem cujo uuid é um template não resolvido) — então a correção exige um mapa curado tipo NON_SPELLCASTER_FOCUS_TRADITION (planVM.ts:3055), mas disparado no ponto de ESCOLHA (chooseFeat/chooseClassChoice), não em applyClass, com ability/tradition verificados contra a regra RAW do PF2e (não existe no pack). Precisa também cobrir o 3º call site (chooseClassLevel) que hoje nem chama buildFocusEntryOp. Teste não-circular: para cada uma das 4 classes, montar o plano até o nível em que o talento/eixo é escolhido, aplicar a escolha, e afirmar que os ops resultantes incluem um doc:create de spellcastingEntry com isFocusPool:true E um doc:update abrindo focusPoints para {1,1} — com ability/tradition esperados hardcoded no teste a partir do texto de regra do PF2e (não lidos do próprio pack, que não carrega esse dado).

---

## #6 — CONFIRMADO (esforço P)

**Medição:** node -e "...spells-core/documents.json..." -> 20 magias com trait 'composition', 10 SEM trait 'focus': Allegro, Courageous Anthem, Dirge of Doom, House of Imaginary Walls, Rallying Anthem, Silver's Refrain, Song of Marching, Song of Strength, Triple Time, Uplifting Overture. Bate exatamente com os dois números da issue (20 e 10) e com a lista de nomes. Medição extra (não citada na issue, relevante para a correção): as 10 têm trait 'cantrip' — não deveriam ganhar 'focus' porque RAW composição-truque não custa Ponto de Foco (mesmo fato que sustenta a #36). SpellPickerDialog.svelte:194-196 filtra `raw.includes(traitFilter)` sem aceitar 'composition'; SpellsTab.svelte:1117 abre o diálogo do Foco com `initialTrait={pickerMode==="focus" ? "focus" : undefined}` fixo — por isso as 10 nunca aparecem em nenhuma lista hoje.

**Arquivos:** `packages/client/src/components/sheets/pf2e/SpellPickerDialog.svelte:194-196`; `packages/client/src/components/sheets/pf2e/SpellPickerDialog.svelte:105,110 (traitFilter seed)`; `packages/client/src/components/sheets/pf2e/SpellsTab.svelte:1117 (initialTrait wiring)`

**Esboço de correção:** Widen o predicado de filtro em SpellPickerDialog.svelte: quando `traitFilter === 'focus'`, aceitar também itens com trait 'composition' (`raw.includes(traitFilter) || (traitFilter==='focus' && raw.includes('composition'))`) — não adicionar 'focus' ao dado, que corromperia a regra de custo (ver #36). Teste NÃO-CIRCULAR: extrair o predicado para uma função pura exportada `matchesTraitFilter(traits, filter)`; ler as traits REAIS de 'Courageous Anthem' e 'Counter Performance' direto de systems/pf2e/packs/spells-core/documents.json (não hardcoded) e assert que ambas retornam true para filter='focus' após o fix, enquanto uma magia sem nenhum dos dois traits (ex. um truque comum) retorna false — a asserção vem da lista real de 20 composições, não de uma cópia da lógica do filtro.

**Acoplada a:** #36

---

## #7 — CONFIRMADO (esforço M)

**Medição:** SpellsTab.svelte:1112 → tradition={pickerMode === 'focus' ? '' : entry.tradition}; linha 1115 → maxRank={pickerMode === 'prepare' ? pickerRank : undefined} (undefined no modo foco). filterSpellPicker (characterSheetVM.ts:2942-2957) só filtra por maxRank/tradition/busca — nenhum filtro por trait de classe. node -e sobre spells-core: 458 magias com trait 'focus', 458/458 com traits.traditions=[] (vazio, 100%), 12/458 com trait 'bard' — todos idênticos aos números da issue. SpellPickerDialog.svelte:105/183-196 confirma que o único filtro de trait é um chip ÚNICO (traitFilter), inicializado em 'focus' mas trocável pelo jogador — não é um filtro composto classe+foco.

**Arquivos:** `packages/client/src/components/sheets/pf2e/SpellsTab.svelte:1108-1122 (props passadas ao picker no modo foco)`; `packages/client/src/components/sheets/pf2e/SpellPickerDialog.svelte:105,183-196 (traitFilter único, sem noção de classe)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:2942-2957 (filterSpellPicker — sem parâmetro de classe/trait de classe)`

**Esboço de correção:** filterSpellPicker precisa de um filtro adicional 'classTrait' (ou reaproveitar traitFilter, mas em AND com 'focus', não em OR/substituição): ao abrir o picker de foco, passar o trait da classe do entry (ex.: Bardo → 'bard') e filtrar entries.traits.value.includes(classTrait) && entries.traits.value.includes('focus'). Como traditions está vazio em 100% das magias de foco (medido), o filtro de tradição não serve pra isso — tem que ser por trait de classe mesmo. Teste não-circular: pegar o array real de spells-core, chamar filterSpellPicker com classTrait='bard'+trait focus, e afirmar que o resultado tem exatamente 12 itens (o número medido agora) e que 'Imposição de Mãos'/'Fatal Aria' (traits champion/bard nível 10) NÃO aparecem para um Bardo nível 1 — a asserção vem da regra RAW (spell de classe X só aparece pra personagem de classe X, nível ≤ metade arredondado pra cima), não da estrutura interna do pack.

---

## #8 — CONFIRMADO (esforço M)

**Medição:** Confirmado como consequência direta de #5: Cleric só ganha a entry 'divine Spells' (isFocusPool=false, prepared.value='prepared') porque hasFocusFeature(Cleric)=false. 138 magias com trait 'cleric' (idêntico à issue). Medição nova: 255 magias NÃO-foco têm tradition 'divine' (ex. Heal), então resolveInitialTradition mantém o filtro 'Divina' ativo por padrão (não cai no fallback de lista vazia) — e como as 138 magias de domínio são todas foco com traditions=[], ficam escondidas até o jogador desligar manualmente o filtro. choiceSetInventory.ts:265 confirma 'feats-core/Domain Initiate/domainInitiate': 'pendente' (linha mudou de 183→265 desde a issue, mesmo conteúdo).

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:2994-3011 (hasFocusFeature bloqueia a entry de foco do Cleric — raiz compartilhada com #5)`; `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts:265 (domainInitiate ainda 'pendente')`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:2440 (addSpellToEntry — aceita a magia de domínio no grimório sem checar isFocusPool do trait 'focus' da magia vs. da entry)`

**Esboço de correção:** Depende estritamente de #5 (dar ao Cleric uma entry de foco) — sem isso, addSpellToEntry não tem pra onde mandar a magia de domínio. Depois de #5 resolvido, adicionar uma guarda em addSpellToEntry (ou na camada de UI que abre o picker): magia com trait 'focus' só pode ir para entry com isFocusPool=true; magia sem 'focus' só para isFocusPool=false — rejeitando/bloqueando a combinação errada no builder. A resolução de QUAL magia de domínio o Cleric ganha automaticamente continua bloqueada pelo ChoiceSet não-convertido (domainInitiate 'pendente') — fora do escopo de planVM puro. Teste não-circular: com a entry de foco do Cleric existindo (pós-#5), chamar addSpellToEntry passando uma magia com trait focus+cleric para a entry 'divine Spells' (isFocusPool=false) e afirmar que a operação é REJEITADA (retorna null/[] ou lança) — a regra vem do RAW (magia de foco nunca ocupa slot preparado), não de nenhum dado do pack.

**Acoplada a:** #5

---

## #9 — CONFIRMADO (esforço P)

**Medição:** Reprodutor EXATO da issue rodado hoje (`node -e` idêntico ao script da issue, varrendo todos os packs): class-features-core 36, todos os demais packs 0. Contagem excluindo entradas deliberadamente marcadas `noDescription:true` (válvula da #27) também dá 36, mesma lista de nomes (Gunslinger/Psychic: Advanced Deed, Gunslinger Expertise, Psychic Spellcasting, Way of the Drifter... 36 no total) — bate EXATAMENTE com o comentário mais recente da própria issue (2026-08-12, build/app@9789baf: '36 de 3.848 entradas').

**Arquivos:** `systems/pf2e/packs/class-features-core/i18n.pt-BR.json (36 entradas Gunslinger/Psychic com description:'')`; `systems/pf2e/packs/class-features-core/documents.json`

**Esboço de correção:** Traduzir as 36 descrições restantes (Gunslinger/Psychic) via o pipeline tools/translate-packs já existente. Teste NÃO-CIRCULAR: gate 'missing-description' (mesmo da #27, tools/translate-packs/src/qa-checks.mjs) deve passar a reportar 0 para class-features-core, e uma leitura direta do documento (ex. 'Advanced Deed') na ficha deve mostrar prosa pt-BR, não fallback EN.

---

## #11 — CONFIRMADO (esforço P)

**Medição:** Medi o VM, não o markup (markup em CharacterSheet.svelte:753-754 confirmado íntegro, `{#each vm.abilities as ability}`). Read characterSheetVM.ts:866-894: `if (!abilities) return [];` (linha 870) roda ANTES do fallback `derivedScores` que a r11.1 introduziu logo abaixo. Rastreei a cadeia: (a) `doc-handlers.ts` cria Actor com o payload do client (ActorDirectory.svelte:159-172) que NÃO inclui `system` nenhum; (b) `derive-runner.ts:29-47` documenta explicitamente 'the PERSISTED/broadcast contract is: only doc.system.derived is ever read back out and written to storage' — os DeriveSteps mutam `system.abilities.<x>.value` num CLONE descartável (stepCharBuildAbilities, build.ts:213-228) que nunca é persistido; só `derived.abilityScores` (character.ts:108,141) sobrevive. Rodei `node -e` contra `CharacterSystemSchema.parse({})` (dist compilado) e confirmei que `abilities` é campo obrigatório sem default — ou seja, um ator novo genuinamente nunca tem `system.abilities` gravado até uma edição manual (`fieldUpdate`, única escrita nesse path, characterSheetVM.ts:2387). Resultado: `this._system["abilities"]` fica undefined para todo ator build-driven, e a lista fica vazia mesmo com `derived.abilityScores` completo.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:866-894 (abilities getter; guard problemático na linha 870)`

**Esboço de correção:** Mover a leitura de `derivedScores` para ANTES do guard e trocar por `if (!abilities && !derivedScores) return [];`. Teste NÃO-CIRCULAR: doc com `system: {}` (sem `abilities`) mas `system.derived.abilityScores = {str:12,dex:10,...}` (fixture simulando o pós-derive real), afirmar que `vm.abilities` retorna 6 linhas com `str.score === 12` — o valor 12 é dado pelo próprio teste como fixture da regra de boost, não lido de `doc.system.abilities` (ausente de propósito no fixture).

---

## #12 — CONFIRMADO (esforço P)

**Medição:** Read planVM.ts:4590-4603 hoje: `function classKeyAbilityOptions(...) { ... return options.length > 1 ? options : undefined; }` — comportamento idêntico ao citado na issue (workaround intacto, só a linha mudou de 3803 para 4590 por crescimento do arquivo). Rodei `node -e "require('./systems/pf2e/packs/classes-core/documents.json').forEach(c=>console.log(c.name,JSON.stringify(c.system.keyAbility)))"`: 7 classes com keyAbility de 1 item (Barbarian[str], Bard[cha], Cleric[wis], Kineticist[con], Rogue[dex], Sorcerer[cha], Wizard[int]) — bate com os '7 classes de chave única' da issue. Todas continuam caindo em `undefined` (grupo sem restrição).

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:4590-4603 (classKeyAbilityOptions)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:4640-4643 (call site em abilityBoostsSlotContext)`

**Esboço de correção:** Trocar `options.length > 1` por `options.length >= 1` (só cai em 'sem restrição' quando a lista vier vazia, ex. Psychic). O comentário que justifica o workaround aponta um bug pré-r11 do applyClass que já foi corrigido (commit b3d083c, r11.1) — a razão de ser do workaround não existe mais. Teste NÃO-CIRCULAR: montar doc com item de classe Bárbaro (`keyAbility:["str"]`, dado real do pack) e chamar `abilityBoostsSlotContext(doc,1)`; afirmar que o grupo `classBoost` tem `allowedSlugs` EXATAMENTE `["str"]` — a expectativa 'str' vem da regra RAW (Bárbaro é chave-Força), não de reler o próprio pack em loop.

---

## #13 — CONFIRMADO (esforço M)

**Medição:** grep -n 'proficiency' planVM.ts → só 2 escritas (linha 3078 em buildSpellcastingEntryOp, linha 3116 em buildFocusEntryOp), ambas 'value: 1' fixo, nenhuma escrita posterior no arquivo. levelSet (planVM.ts:5057-5090) só sincroniza system.slots via syncSlotMaxOp, filtrando explicitamente as entries de foco (linha 5076: isFocusPool !== true) e NUNCA toca em system.proficiency de nenhuma entry. Achado adicional (não estava na issue): o mecanismo de progressão JÁ EXISTE no pack — classSystem.proficiencyUpgrades tem entradas com stat:'spellcasting' para 7 das 12 classes (Bard, Champion, Druid, Magus, Psychic, Sorcerer, Wizard — verificado via node -e sobre classes-core/documents.json), e o servidor já usa esse MESMO mecanismo (effectiveRank, build.ts:243-256) para armas/saves/perceção/classDC em stepCharApplyClass — só não é aplicado a spellcastingEntry. stepCharSpellcasting (spellcasting.ts:45-53,94) lê system.proficiency do ITEM literalmente (readProficiencyRank), nunca deriva de proficiencyUpgrades. Gap adicional descoberto: Cleric não tem NENHUMA entrada stat:'spellcasting' em proficiencyUpgrades no pack — mesmo uma correção correta do código deixaria Cleric sem progressão (falta de dado, não é a issue #13 em si).

**Arquivos:** `systems/pf2e/src/derivations/spellcasting.ts:45-53 (readProficiencyRank — lê o valor congelado do item)`; `systems/pf2e/src/derivations/spellcasting.ts:74-116 (stepCharSpellcasting — precisa computar rank via proficiencyUpgrades em vez de ler do item)`; `systems/pf2e/src/derivations/build.ts:243-256 (effectiveRank — mecanismo já existente a ser reaproveitado)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:3078,3116 (valor inicial 'trained' em nível 1 — correto, não precisa mudar)`

**Esboço de correção:** A pista da própria issue está certa: existe 'um lugar único onde a progressão de rank por nível deveria viver' (build.ts's effectiveRank) e a issue está sendo resolvida no lugar errado hoje. Fix real é SERVER-SIDE, não em planVM.ts: em stepCharSpellcasting, para cada spellcastingEntry, achar a classe dona (single-class = a única; multiclasse = via flags.fusion.classKey já gravado por chooseClassLevel/buildSpellcastingEntryOp) e calcular rank = effectiveRank(1, 'spellcasting', classSystem.proficiencyUpgrades, level) em vez de readProficiencyRank(itemSys). planVM.ts não precisa mudar (value:1 em nível 1 já está certo — o item guarda o valor de criação, o servidor é quem deveria sobrescrever no derive). Teste não-circular: montar um actor Wizard nível 20 com spellcastingEntry.system.proficiency.value=1 (como planVM de fato emite), rodar stepCharSpellcasting, e afirmar system.derived.spellcasting[entryId].rank === 4 (legendary) — o '4' vem da regra RAW do PF2e (Wizard é lendário em conjuração a partir do nível 19), não do dado armazenado no item.

---

## #18 — CONFIRMADO (esforço G)

**Medição:** Bundle real: checkFeatPrerequisites com personagem nível 20 SEM nenhum item, para 9 classes (fighter/ranger/monk/champion/wizard/sorcerer/bard/rogue/barbarian) -> 499 entradas de system.prerequisites medidas, 0 marcadas (issue citava 458/0 — a diferença é o conjunto de classes escolhido, a conclusão '0 marcações' é idêntica). Casos nominais da issue confirmados intactos: Reflecting Riposte, Crane Flutter (exige Crane Stance), Side by Side, Greater Mercy (exige Mercy), Courageous Onslaught, Archwizard's Might, Greater Crossblooded Evolution -> todos undefined (sem marca) mesmo sem o talento-base. Código mantém o comentário original ('deliberately NOT a general feats-core lookup, since this VM has no pack index') sem alteração — nenhum PR tocou esse trecho. O #41 (fechado) publicou sourceId em indexFields pra resolver overlay i18n em spellHeal.ts (uuidBySourceId) — não dá a derivePlan (função PURA, síncrona, chamada sem socket) acesso a um índice de nomes de feats-core; o gap é arquitetural (VM síncrona vs. compendiumApi.ts assíncrono-via-socket), não de campo ausente no índice. O obstáculo descrito na issue #18 continua válido hoje.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:2476-2506 (knownPossessedNames — só olha itens do PRÓPRIO personagem, nunca o pack)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:2530-2561 (evaluatePrerequisiteCandidate — candidato que não é axis e não está em knownNames sempre vira 'unresolved', nunca 'unmet')`; `packages/client/src/lib/sheets/pf2e/planVM.ts:1171 (derivePlan — assinatura só recebe doc, sem socket/índice)`; `packages/client/src/lib/compendium/compendiumApi.ts:121 (getPackIndex — único jeito hoje de obter um índice do pack, é assíncrono e exige Socket)`

**Esboço de correção:** Precisa de um índice nome->doc de feats-core/class-features-core disponível para o VM ANTES de marcar 'unmet' por nome ausente — sem ele, o módulo não consegue distinguir 'nome de talento real que falta' de prosa não-nominal (proficiência, atributo), e marcar às cegas violaria DEC-BC-05 (falso unmet é pior que nenhuma marca). Caminho: o componente que já chama derivePlan (PlanColumn.svelte) busca o índice de feats-core via compendiumApi.getPackIndex uma vez (cacheável — pack não muda em runtime) e passa um Set<string> de nomes conhecidos para checkFeatPrerequisites como parâmetro opcional; quando presente, um candidato não-axis que não bate em knownNames NEM em packNames vira 'unresolved' ainda (prosa), mas um que bate em packNames-mas-não-em-knownNames vira 'unmet'. Teste NÃO-CIRCULAR que prova: fixture com o índice real (nomes carregados de systems/pf2e/packs/feats-core/documents.json) + personagem sem 'Crane Stance' -> checkFeatPrerequisites(craneFlutter,...) deve retornar PrerequisiteUnmet citando 'Crane Stance' — a asserção vem da regra PF2e (Crane Flutter exige Crane Stance), não de uma tabela inventada no teste.

---

## #20 — CONFIRMADO (esforço M)

**Medição:** Grep em CompendiumPickerDialog.svelte por requirementIssue/checkFeatPrerequisites/checkSlotRequirement/prerequisite -> 0 ocorrências. O único predicado usado na lista do picker é filterFn (linha 129, 'Non-blocking eligibility predicate — ineligible entries are hidden'), e em PlanColumn.svelte:pickerConfigFor (linha ~949-960) esse filterFn chama isFeatEligible — que não olha system.prerequisites em nenhum branch (confirmado lendo planVM.ts:2030-2088). attachRequirementIssues (a função que de fato chama checkFeatPrerequisites) só é chamada em derivePlan sobre SLOTS JÁ PREENCHIDOS (planVM.ts:1561), nunca sobre os candidatos do picker. Nenhum dos PRs #111/#112 tocou CompendiumPickerDialog.svelte ou PlanColumn.svelte — confirmado pela lista de arquivos de cada PR. O gap descrito na issue #20 continua válido hoje.

**Arquivos:** `packages/client/src/components/sheets/pf2e/plan/CompendiumPickerDialog.svelte:57,129 (filterFn — só esconde, não marca)`; `packages/client/src/components/sheets/pf2e/plan/PlanColumn.svelte:946-961 (pickerConfigFor para classFeat — filterFn = isFeatEligible, sem checkFeatPrerequisites)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:2599-2625 (checkFeatPrerequisites — já existe e funciona, só falta ser chamado no contexto do picker)`

**Esboço de correção:** CompendiumPickerDialog precisa de um segundo predicado (não-bloqueante, só decorativo) além do filterFn de elegibilidade: para cada entry ainda na lista, chamar checkFeatPrerequisites(featDocFromIndex(e), itemsDoPersonagem, classSystem, effectiveLevel) e, se retornar issue, renderizar o mesmo aviso visual que LevelCard.svelte já usa para slots preenchidos (mesma reasonKey/params, mesmo componente de aviso). Isso depende de #18 estar em andamento junto para valer a pena (senão a maioria dos prereqs continua 'unknown' e o aviso raramente aparece), mas é tecnicamente independente — pode ser feito com a cobertura de hoje (#19/#21/#31 já dão sinal em eixos) como primeira fatia. Teste não-circular: dado um personagem Bárbaro com instinto errado e a lista de candidatos incluindo 'Draconic Arrogance' (prereq 'dragon instinct'), o snapshot/props do picker precisam expor requirementIssue!==undefined para esse candidato ANTES de confirmar a escolha — reproduzindo exatamente o cenário do aud-36/aud-38 citado na issue.

**Acoplada a:** #18

---

## #26 — NUMERO_DESATUALIZADO (esforço P)

**Medição:** node -e sobre feats-core: dos 4 pré-requisitos quebrados citados na issue, 3 JÁ FORAM CORRIGIDOS — Elemental Fist@2 e Ki Cutting Sight@6 agora citam 'Qi Spells' (existe, era 'Inner Upheaval'); Vitality-Manipulating Stance@20 agora cita 'Stunning Blows' (existe, era 'Stunning fist' — e confirmei que a string 'Stunning fist'/'Stunning Fist' já não aparece em pré-requisito nenhum do pack). Resta 1/4 quebrado: Endurance of the Rooted Tree@20 (capstone) ainda tem prerequisites:[{value:'Wholeness of Body'}] — esse nome NÃO existe como documento; a descrição do próprio talento cita @UUID[...Item.Harmonize Self]{Wholeness of Body} (nome de exibição legado vs. doc real 'Harmonize Self', que existe em feats-core nível 4). Título da issue ('Stunning Fist e Wholeness of Body não existem') está parcialmente desatualizado — Stunning Fist já não é problema.

**Arquivos:** `systems/pf2e/packs/feats-core/documents.json (doc 'Endurance of the Rooted Tree', campo system.prerequisites — trocar 'Wholeness of Body' por 'Harmonize Self')`

**Esboço de correção:** Editar o pack (curation, não runtime): normalizar prerequisites de 'Endurance of the Rooted Tree' de 'Wholeness of Body' para 'Harmonize Self', igual já foi feito para os outros 3 (Inner Upheaval->Qi Spells, Stunning fist->Stunning Blows). Teste NÃO-CIRCULAR seguindo o padrão de prereqEvaluator.test.ts: para o talento 'Endurance of the Rooted Tree' extraído do pack real, montar um personagem com 'Harmonize Self' na lista de talentos conhecidos e afirmar evaluatePrerequisiteEntry(...) === 'met' — hoje esse mesmo teste com o dado atual do pack falharia (fica 'unresolved'/sem marca), provando o defeito antes do fix.

---

## #28 — NUMERO_DESATUALIZADO (esforço M)

**Medição:** node -e sobre feats-core/class-features-core: dos 6 alvos citados na issue, 4 JÁ FORAM CORRIGIDOS — Vengeful Oath@2 agora cita 'Justice' (existe em class-features-core), Lasting Doubt@12 agora cita 'Redemption' (existe), Liberating Stride@12 agora cita 'Liberation' (existe), Aura of Vengeance@14 agora cita ['Exalted Reaction','Vengeful Oath'] e 'Exalted Reaction' existe em class-features-core l11 (era 'Exalt', que não existia) — toda a cadeia resolve hoje. Restam 2/6 quebrados: Anchoring Aura@14 e Banishing Blow@20 (capstone) ainda citam prerequisites:[{value:'Fiendsbane Oath'}], e confirmei por busca exaustiva em feats-core/class-features-core/actions-core/equipment-core que NENHUM documento chamado 'Fiendsbane Oath' existe em pack nenhum (só aparece como slug 'feat:fiendsbane-oath' dentro de um predicate de rule element em actions-core, não como doc importável).

**Arquivos:** `systems/pf2e/packs/feats-core/documents.json (Anchoring Aura, Banishing Blow — dependem de um doc 'Fiendsbane Oath' que precisa ser IMPORTADO do vendor, não é caso de rename)`

**Esboço de correção:** Diferente de Vengeful/Lasting/Liberating (que eram rename), aqui falta IMPORTAR o documento 'Fiendsbane Oath' (juramento do Champion) do vendor pf2e/packs para feats-core — não há doc equivalente já presente sob outro nome. Depois de importado, os 2 pré-requisitos passam a resolver sem qualquer mudança de código. Teste NÃO-CIRCULAR: mesmo padrão de prereqEvaluator.test.ts — montar personagem com 'Fiendsbane Oath' na lista de talentos e afirmar 'met' para Anchoring Aura/Banishing Blow; falha hoje (unresolved) porque o doc não existe no pack.

---

## #29 — CONFIRMADO (esforço M)

**Medição:** Bundle real, checkFeatPrerequisites sem nenhum item possuído: Heal Companion ('an animal companion'), Side by Side Ranger ('an animal companion'), Magic Hide/Enlarge Companion ('an animal companion'+'warden spells'), Warden's Focus/Snare Hopping/Natural Conduit/Ranger's Bramble ('warden spells'), Animal Strength ('animal feature warden spell') -> TODOS undefined (nenhuma marca), confirmando 0 marcações também hoje. Nenhuma tabela de alias para essas frases existe em planVM.ts (grep 'animal companion'/'warden spells'/'ALIAS' -> 0 ocorrências). Nenhum dos PRs recentes tocou esse caso.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:2476-2506 (knownPossessedNames — candidato alvo)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:2530-2561 (evaluatePrerequisiteCandidate — onde a tabela de alias entraria, antes do fallback 'unresolved')`

**Esboço de correção:** Tabela de alias curada (pequena, só para o Ranger): {'an animal companion': 'Animal Companion (Ranger)', 'warden spells': 'Initiate Warden'} normalizados, consultada em evaluatePrerequisiteCandidate ANTES do fallback 'unresolved' — se o candidato normalizado bate uma chave do alias, resolve para o nome do concessor real e reusa a checagem de knownNames existente (met se o personagem tem o concessor, senão segue para unmet SE #18 também estiver resolvida, ou fica no comportamento atual se não). Pode ser entregue independente de #18 já como uma extensão pontual do bloco de aliases existente (mesma forma de UNMODELED_AXIS_PHRASES, mas resolvendo para um nome em vez de 'unmet' direto). Teste não-circular: Ranger nível 8 SEM 'Animal Companion (Ranger)' escolhendo 'Enlarge Companion' (prereq 'an animal companion'+'warden spells') deve dar PrerequisiteUnmet citando o concessor ausente — a asserção vem da regra real do Ranger (precisa ter o talento de companheiro antes de melhorá-lo), não de um valor hardcoded no teste.

**Acoplada a:** #18

---

## #30 — CONFIRMADO (esforço M)

**Medição:** node -e sobre feats-core: Master of Many Styles@16 mantém prerequisites=[{value:'Opening Stance (Fighter)'},{value:'Reflexive Stance (Monk)'}] — duas entradas separadas (AND). 'Opening Stance' existe com trait fighter; 'Reflexive Stance' existe só com trait monk, SEM o sufixo '(Monk)' citado no texto — confirma a leitura da issue de que o grafo só casa via stripParentheticalSuffix. Não achei nenhuma mudança de código desde a issue que trate isso como OR — grep em planVM.ts não retorna tratamento especial para esse par. Qi Center@18 e Immortal Techniques@20 continuam dependendo de Master of Many Styles, então a cadeia de 3 talentos do Monk segue órfã como descrito.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts (evaluatePrerequisiteEntry — hoje trata entradas separadas do array prerequisites sempre como AND; precisaria de uma exceção nomeada, no padrão UNMODELED_AXIS_PHRASES/#45, para este par específico se a decisão for modelar como OR)`; `systems/pf2e/packs/feats-core/documents.json (doc 'Reflexive Stance' l12 [monk] — decidir se precisa de uma segunda versão Fighter, ou se o pré-requisito do Monk é o que deveria mudar)`

**Esboço de correção:** Pré-requisito de decisão de produto ANTES de codar (a própria issue marca isso como não confirmado contra a fonte): 1) confirmar no vendor/fonte oficial se existe uma 'Reflexive Stance (Fighter)' separada que não foi importada, ou se o pré-requisito do Monk deveria ser tratado como OR entre duas vias de acesso à mesma stance. 2) Se for OR: seguir o padrão de UNMODELED_AXIS_PHRASES (planVM.ts) para reconhecer esse par nomeado e avaliar como alternativa em vez de conjunção. Teste NÃO-CIRCULAR: com um Monge que possui 'Reflexive Stance' mas não 'Opening Stance (Fighter)', afirmar que Master of Many Styles fica 'met' (hoje fica 'unmet'/sem marca por exigir as duas) — a asserção vem da regra pretendida (via disjunção), não de reconferir o pack.

---

## #33 — CONFIRMADO (esforço M)

**Medição:** `node -e` no doc 'Bloodline: Draconic' de class-features-core: Object.keys(system) = [actionType,actions,category,description,level,prerequisites,publication,rules,traits] — idêntico ao que a issue cita, nenhum campo estruturado de tradição foi adicionado. `grep traditionByBloodline` só aparece em planVM.ts (para a entry de conjuração da classe, via resolveBloodlineTradition:3946-3953) e no teste — NÃO existe nenhuma leitura de 'bloodline that grants X spells' em checkFeatPrerequisites/planVM.ts (grep vazio), então os 6 talentos dependentes (Blessed Blood, 4x Evolution, Soulsight) continuam SEM MARCA como a issue descreve.

**Arquivos:** `systems/pf2e/packs/class-features-core/documents.json (18 docs 'Bloodline: *')`; `packages/client/src/lib/sheets/pf2e/planVM.ts:3946-3953 (resolveBloodlineTradition, só usado pra conjuração da classe)`; `tools/importer-pf2e/src/curation/classes/sorcerer.json (traditionByBloodline já existe como fonte)`

**Esboço de correção:** Reaproveitar a mesma tabela traditionByBloodline do curation/sorcerer.json (já usada por resolveBloodlineTradition) dentro de checkFeatPrerequisites/prereqEvaluator para resolver o padrão 'bloodline that grants <tradition> spells'. Teste: Sorcerer Draconic escolhe Divine Evolution (pré-requisito 'bloodline that grants divine spells') → deve marcar MET apenas se o dragão escolhido (ex. Adamantine) mapear para tradition='primal'... e UNMET para Draconic padrão (tradition variável) — comparado à tabela RAW Draconic Exemplars, não ao próprio traditionByBloodline.

**Acoplada a:** #53

---

## #34 — CONFIRMADO (esforço G)

**Medição:** CLASS_CHOICE_SLOTS (planVM.ts:923-939) não tem entrada para 'Divine Font' (só 'Doctrine' pro Cleric) — confirmado por leitura direta do objeto. choiceSetInventory.ts:115 confirma 'class-features-core/Divine Font/divineFont': 'pendente'. Medição de prerequisitos DIVERGE da issue: node -e sobre feats-core encontrou 19 entradas de prereq citando font (não 17) em 18 talentos distintos (não 16) — 2 a mais que a issue lista: 'Bless Tonic' ('deity who grants heal divine font') e 'Bless Toxin' ('deity who grants harm divine font'), que não estavam na lista original. Total de entradas de prereq da classe (trait cleric) segue em 60, igual à issue. Ou seja: o problema não diminuiu, PIOROU (17/60=28% → 19/60=31.7%) — mais talentos passaram a citar font desde que a issue foi aberta.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:923-939 (CLASS_CHOICE_SLOTS — falta 'Divine Font')`; `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts:115 (divineFont ainda 'pendente')`; `systems/pf2e/packs/feats-core/documents.json (18 talentos com prereq citando font, incl. os 2 novos: Bless Tonic, Bless Toxin)`

**Esboço de correção:** Precisa de DADO que hoje não existe em pack nenhum (as 2 opções Harm Font/Heal Font não são documentos separados) E de um slot no builder. Passo 1 (dado): curar 2 pseudo-opções 'Heal Font'/'Harm Font'/'Versatile Font' (RAW: escolher Heal, Harm, ou ambos com metade dos slots) como entradas navegáveis, análogo ao que já foi feito pra Doctrine. Passo 2 (builder): adicionar 'Divine Font' ao CLASS_CHOICE_SLOTS com um novo PlanSlotType (ex. 'divineFont'), e um chooseClassChoice-like handler que grava a escolha em system.build.choices para então os 16-18 talentos com prereq citando font resolverem via a MESMA lógica de resolução de pré-requisito já usada pra Doctrine. Teste não-circular: escolher 'Heal Font' no slot, então avaliar o prereq de 'Healing Hands' (que exige 'healing font') via prereqEvaluator — afirmar 'met', e o de 'Harming Hands' ('harmful font') — afirmar 'unmet' — a asserção vem do texto RAW do talento, lido do próprio prerequisites.value do pack (não inventado), contra a escolha feita, que é o padrão não-circular do prereqEvaluator.test.ts.

---

## #36 — CONFIRMADO (esforço M)

**Medição:** Leitura direta de SpellsTab.svelte:698-707 (linha da issue, 685-687, mudou de número mas é a mesma função): `castFocusSpell` chama `vm.setFocusPoints(Math.max(0, vm.focusPoints.value - 1))` incondicionalmente para qualquer spellId, sem checar trait. Confirmado que SpellRow (characterSheetVM.ts:399-414) não carrega nenhuma informação de trait/custo, e a getter `focusSpells` (linha ~1549-1604) calcula `isFocusTrait` internamente (linha 1577) mas DESCARTA essa informação antes de montar o SpellRow (linhas 1592-1600) — o componente não tem como saber, hoje, se a magia clicada custa foco.

**Arquivos:** `packages/client/src/components/sheets/pf2e/SpellsTab.svelte:698-707 (castFocusSpell) e 1082 (call site)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:399-414 (SpellRow interface)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:1549-1604 (focusSpells getter — já calcula isFocusTrait na linha 1577, só precisa propagar)`

**Esboço de correção:** Adicionar `costsFocusPoint: boolean` ao SpellRow, preenchido com `isFocusTrait` na getter `focusSpells` (characterSheetVM.ts:1592-1600). Melhor ainda, seguindo o padrão do repo (lógica de negócio na VM, não no componente): expor um método `vm.castFocusSpell(spellId)` que lê o item embutido, decide internamente se decrementa, e devolve o op (ou null) — o componente vira um passthrough fino, testável sem Svelte. Teste NÃO-CIRCULAR (mesmo padrão de focusPool.test.ts): montar um actor doc com dois itens de magia embutidos usando os traits REAIS puxados de spells-core (Courageous Anthem — cantrip, sem focus; Counter Performance — com focus) e pool {value:1,max:1}; chamar o método para cada uma; assert que castar Courageous Anthem mantém focusPoints.value===1 (a regra é 'cantrip não custa foco', lida do próprio trait do pack) enquanto castar Counter Performance derruba para 0.

**Acoplada a:** #6

---

## #37 — CONFIRMADO (esforço G)

**Medição:** classes-core/documents.json confirma Bard.system.spellcasting.type='spontaneous' — e planVM.ts:3075 (`prepared: { value: spellcasting.type }`) já grava isso CORRETAMENTE no entry criado; a 'pista' da issue (builder pode não estar gravando spontaneous) está descartada por medição. O bug real é mais fundo: `grep 'entry.prepared'` (ou qualquer leitura de `.prepared`) em SpellsTab.svelte = 0 ocorrências — SpellcastingEntryRow.prepared (characterSheetVM.ts:366) existe e chega correto ao componente, mas NUNCA é lido no template. O bloco `{#if activeTab?.kind === 'entry'}` (linha 832) renderiza incondicionalmente, para QUALQUER entry (prepared, spontaneous ou innate): a seção de slots por rank com 'Preparar do grimório…' (linhas 901-988, via `vm.getPreparedSlot`) e a seção 'Grimório'/'+Adicionar magia' (990+). Confirmado também que não existe nenhum affordance de 'adicionar truque': `cantrips(entry)` (linha 744) só lê o que já está embutido — sem botão de adicionar, então a seção de truques fica invisível (length 0) até o jogador ganhar cantrips por outro caminho, o que hoje não existe para entries spontaneous/prepared genéricas.

**Arquivos:** `packages/client/src/components/sheets/pf2e/SpellsTab.svelte:832-1020 (branch por entry.prepared: slots-por-rank+grimório para 'prepared'; lista de conhecidas+cast direto para 'spontaneous'/'innate')`; `packages/client/src/components/sheets/pf2e/SpellsTab.svelte:744-747 (cantrips) — precisa de affordance de adicionar para spontaneous`; `packages/client/src/lib/i18n/pt-BR.json e en.json — novas chaves (ex. Spells.KnownSpells / Spells.AddCantrip) ao lado de Spells.Grimoire/Spells.PrepareEllipsis já existentes`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts — SpellcastingEntryRow.prepared já correto (366, 1375), nenhuma mudança de dado necessária, só um helper novo tipo `knownSpellsByRank(entry)` se o fix quiser reaproveitar SpellRow[] por rank`

**Esboço de correção:** No template, trocar `{#each entry.slots.filter(...)}`/'Grimório' por um branch em `entry.prepared`: 'prepared' mantém a UI atual (slots+grimório); 'spontaneous'/'innate' renderiza cada slot.spells (já agrupado por rank, characterSheetVM.ts:1402+) como lista direta com botão Lançar (sem estado vazio/preparar), e o botão de adicionar convocaria o picker sem o passo de 'preparar'. Adicionar affordance de '+ truque' reaproveitando `openAddPicker` com rank=0. Teste NÃO-CIRCULAR: como o repo não tem harness de render Svelte (`@testing-library/svelte` ausente do package.json do client), extrair a decisão pura `spellSurfaceModeFor(prepared: string): 'slots'|'known-list'` para characterSheetVM.ts e testar contra os valores REAIS de `spellcasting.type` lidos de classes-core (Cleric/Druid/Magus/Wizard='prepared', Bard/Psychic/Sorcerer='spontaneous', todo Focus entry='innate' via buildFocusEntryOp) — a asserção vem da tabela real do pack, não de uma reafirmação do texto da issue.

---

## #38 — CONFIRMADO (esforço M)

**Medição:** Read character.ts:328-372 (stepCharSaves) e :385-412 (stepCharPerception): ambos escrevem `derived.saves[name]`/`derived.perception` só com `{slug,base,modifiers,total,dc}` (tipo `DerivedStatistic`, types.ts:44-54) — SEM rank, embora o rank já esteja calculado localmente (`sysSaves[saveName]?.rank`, `sys.perception?.rank`). Read characterSheetVM.ts:939-958 (saves getter) confirma o consumo: `const rank = savesSource?.[name]?.rank ?? 0` lê DIRETO de `this._system["saves"]` (bruto), nunca de `derived`; idem `_perceptionRank()` (921-924) lê só `system.perception.rank`. O mesmo bug JÁ foi corrigido para skills — types.ts:56-75 documenta literalmente esse padrão ('a persisted rank stays 0 while total already includes the proficiency bonus') e characterSheetVM.ts:1049 já faz `derivedStat?.rank ?? raw?.rank ?? 0` para skills. git log não mostra nenhum commit equivalente tocando saves/perception.

**Arquivos:** `systems/pf2e/src/derivations/character.ts:328-372 (stepCharSaves)`; `systems/pf2e/src/derivations/character.ts:385-412 (stepCharPerception)`; `systems/pf2e/src/derivations/types.ts:44-75 (DerivedStatistic / DerivedSkillStatistic — padrão a replicar)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:900-924 (perception getter / _perceptionRank)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts:939-958 (saves getter)`

**Esboço de correção:** Espelhar o padrão de skills: stepCharSaves/stepCharPerception passam a gravar `rank` junto do resto (reaproveitando `DerivedSkillStatistic`-like shape ou um novo tipo pequeno). No client, `saves` getter e `_perceptionRank()` passam a preferir `derived.saves[name].rank ?? savesSource?.[name]?.rank ?? 0` (mesma linha do getter de skills, characterSheetVM.ts:1049). Teste NÃO-CIRCULAR: doc com classe Bárbaro embutida (savingThrows.fortitude=2 no pack = Expert) e SEM `system.saves` manual, rodar `runCharacterPipeline`, afirmar `derived.saves.fortitude.rank === 2` — o 2 vem da regra RAW (Bárbaro é Expert em Fortitude no nível 1), não de reler `system.saves` (que é justamente o campo que fica 0 hoje).

---

## #39 — CONFIRMADO (esforço P)

**Medição:** Read build.ts:573-628 (stepCharBuildHp). Linha 587: `if (levels.classes.size === 0) return;` já garante que só roda com classe embutida. Linha 590 (gate extra e redundante): `if (ancestryHp === undefined && !sys.build) return;`. Logo após confirmar SÓ a classe (sem ancestralidade escolhida, sem nenhuma escolha de build gravada ainda), `findAncestryHp(doc)` retorna undefined e `sys.build` ainda não existe (só é criado quando a PRIMEIRA escolha de build é persistida) — as duas condições do `&&` se satisfazem e o step aborta, deixando `system.attributes.hp` no default (0/0). Isso bate exatamente com os 3 casos da issue: Bardo 8/8, Campeão 10/10, Bárbaro 12/12 aparecem só depois de UMA escolha qualquer criar `system.build` — e esses números são exatamente classHp*1 nível + 0 CON + 0 ancestralidade, confirmando a hipótese.

**Arquivos:** `systems/pf2e/src/derivations/build.ts:573-628 (stepCharBuildHp; gate problemático na linha 590)`

**Esboço de correção:** Remover/afrouxar o gate da linha 590 — ele é redundante com o `classes.size === 0` de duas linhas acima e é a única razão do 0/0. Trocar por um gate que dependa só de haver classe (mesmo critério que `stepCharApplyClass`, build.ts:280-297, já usa sem essa restrição extra). Teste NÃO-CIRCULAR: doc com item de classe Bárbaro (classHp=12 no pack), SEM item de ancestralidade e SEM `system.build`, rodar `runCharacterPipeline`, afirmar `derived.hp.max === 12` — 12 é o HP conhecido de um Bárbaro nível 1 sem CON/ancestralidade (regra RAW), reproduzindo os valores vistos nos `aud-*.yaml` só depois de outra escolha.

---

## #42 — CONFIRMADO (esforço M)

**Medição:** A infraestrutura server-side do 'plano (b)' que a issue cita EXISTE hoje: `compendium:i18nBySourceRef` (packages/shared/src/compendium.ts:332-355, service.ts:338-347 getI18nBySourceRef, handlers.ts:165-186, registrado em socket-manager.ts:465) — mas `grep -ri i18nBySourceRef` em todo packages/client/src não retorna NENHUM resultado: nenhum componente do cliente chama esse socket event. Ou seja, itens já gravados com i18n.ptBR.description='' (o caso concreto da issue: Stumbling Stance, Bard, Maestro, Lingering Composition no ator 'Novo Ator') continuam mostrando EN hoje, porque nada no cliente ainda troca a leitura do i18n embutido (stale) pela resolução via sourceRef.

**Arquivos:** `packages/shared/src/compendium.ts:332-355`; `packages/server/src/compendium/service.ts:338-347 (já pronto)`; `packages/client/src/lib/compendium/documentDetails.ts (falta consumir o socket aqui)`; `packages/client/src/lib/sheets/pf2e/characterSheetVM.ts (falta o fetch pathway)`

**Esboço de correção:** Wire o cliente: quando um item embutido tem i18n.ptBR.description vazia/ausente, chamar `compendium:i18nBySourceRef` com `item.flags.fusion.{packName,sourceId}` e usar o resultado no lugar do overlay persistido. Teste NÃO-CIRCULAR: ator com item i18n.ptBR.description='' + flags.fusion apontando pra um doc do pack cuja overlay TEM prosa pt-BR real → a descrição renderizada na ficha deve ser a do PACK (texto verificável no próprio i18n.pt-BR.json do pack), não a string vazia persistida no item.

**Acoplada a:** #43

---

## #43 — CONFIRMADO (esforço M)

**Medição:** `grep 'delete worldDoc\[' packages/server/src/compendium/service.ts` → linhas 451-453: `delete worldDoc["uuid"]; delete worldDoc["i18n"]; delete worldDoc["mechanics"];` ainda acontece hoje (deliberado, comentário explícito 'issue #43 — must not be reverted', linha 441). A PARTE NOVA: o leitor que a issue pede ('falta o LEITOR resolver o overlay pelo pack de origem em tempo de leitura') já foi CONSTRUÍDO no servidor — `compendium:i18nBySourceRef` (service.ts:338-347, handler registrado em socket-manager.ts:465, testado em compendium-source-ref.test.ts com 13 casos incluindo desambiguação cross-pack) — mas nenhum componente do cliente o consome ainda (mesmo grep vazio da #42). O bug fim-a-fim (item importado pro mundo mostra EN pra sempre) continua verdadeiro na prática, mas a peça que faltava do lado do servidor está pronta.

**Arquivos:** `packages/server/src/compendium/service.ts:338-347,451-453`; `packages/server/src/compendium/handlers.ts:165-186`; `packages/server/src/net/socket-manager.ts:465`; `packages/server/src/__tests__/compendium-source-ref.test.ts`; `packages/client/src (nenhum consumidor ainda)`

**Esboço de correção:** Mesmo fix da #42 (mesma peça de wiring cliente-servidor, ambas as issues compartilham a solução) — chamar `compendium:i18nBySourceRef` no cliente ao ler um item de mundo sem overlay embutido válido. Teste: importar um doc do compêndio pro mundo (via CompendiumService.importToWorld, que já remove i18n) e então ler sua descrição pela ficha — deve mostrar pt-BR resolvido via sourceRef, não EN puro.

**Acoplada a:** #42

---

## #46 — NUMERO_DESATUALIZADO (esforço P)

**Medição:** node -e sobre feats-core: item 1 (Occult Evolution) JÁ FOI CORRIGIDO — prerequisites hoje é 'bloodline that grants occult spells' (era 'bloodline the grants...'), igual aos 3 irmãos (Arcane/Divine/Primal Evolution). Item 2 (Echoing Channel) SEGUE QUEBRADO: prerequisites ainda é 'Embodiment of Balance or Cleric' — confirmei por busca exaustiva em feats-core+class-features-core+actions-core que nem 'Embodiment of Balance' nem 'Cleric' existem como nome de documento. Título da issue cita os DOIS casos; hoje só 1/2 persiste.

**Arquivos:** `systems/pf2e/packs/feats-core/documents.json (doc 'Echoing Channel', system.prerequisites — texto de prosa do vendor, não tem alvo resolvível hoje)`

**Esboço de correção:** Baixa gravidade e sem impacto funcional hoje (nenhuma camada avalia essa string — confirmado pela própria issue). Se algum dia um avaliador de pré-requisito prosa-livre for construído, esse texto precisa de tratamento especial (não é nome de doc, é frase quebrada do vendor: 'Cleric' sozinho não é um talento/feature). Teste NÃO-CIRCULAR só faz sentido quando essa camada existir; até lá, o assert mecânico possível é apenas de regressão: confirmar que o texto de prerequisites de 'Echoing Channel' permanece STRING (não veio a virar um array malformado) — não prova nada sobre resolução, só sobre integridade do dado.

---

## #49 — CONFIRMADO (esforço M)

**Medição:** Read planVM.ts:4276-4354 (skillTrainingDialogContext). Linhas 4335-4336 hoje: `const eligible = kind === "skillTraining" ? currentRank === 0 : currentRank >= 1 && currentRank < 4;` — nenhuma referência a `charLevel` (que já está disponível na função, linha 4283). Grep em build.ts confirma que `stepCharBuildSkills` (477-540) também não checa nível: `targetRank = choice.rank ?? (... : Math.min(current + 1, 4))` (linha 536) só limita em 4 (Lendário), sem gate de nível. Grep por `RANK_MIN_LEVEL`/`MASTER_LEVEL`/`LEGENDARY_LEVEL` no repo: nenhum resultado — não existe hoje NENHUM teto de nível em lugar nenhum do código, confirmando a issue tal como está.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:4276-4354 (skillTrainingDialogContext; eligible em 4335-4336)`; `systems/pf2e/src/derivations/build.ts:477-540 (stepCharBuildSkills; targetRank em 526-538)`

**Esboço de correção:** Duas frentes (a própria issue já pede as duas): (1) em `skillTrainingDialogContext`, adicionar `charLevel >= 7` como pré-condição para oferecer rank 3 (Mestre) e `charLevel >= 15` para rank 4 (Lendário); (2) o mesmo teto em `stepCharBuildSkills` antes de aplicar `targetRank` via `setFloor`, para a persistência não aceitar por outro caminho (ex. um `choice.rank` explícito). Introduzir constantes `SKILL_MASTER_MIN_LEVEL=7`/`SKILL_LEGENDARY_MIN_LEVEL=15`. Teste NÃO-CIRCULAR: Ladino nível 8 com a sequência de `skillIncreaseLevels` que hoje leva uma perícia a Lendário no nível 8 (o próprio exemplo da issue) — depois da correção, a mesma sequência de choices deve produzir rank 3 (Mestre), não 4, porque nível 8 < 15; a asserção usa o número 15 da regra oficial do PF2e, não dado do pack.

---

## #50 — CONFIRMADO (esforço G)

**Medição:** node -e sobre classes-core/documents.json: Cleric.system.proficiencyUpgrades tem EXATAMENTE 4 entradas — {level:5,stat:perception,rank:2}, {level:9,stat:will,rank:3}, {level:11,stat:reflex,rank:2}, {level:13,stat:'armor.unarmored',rank:2} — bate com o '4' da issue. Nota 5 do curation file (tools/importer-pf2e/src/curation/classes/cleric.json) confirma a causa citada na issue: `deriveProficiencyUpgrades` (proficiency-upgrades.mjs:170-203) itera só `classItemsMap()`/`featureByName()`, e para 'First Doctrine'..'Final Doctrine' isso resolve para os WRAPPERS vazios (subfeatures.proficiencies={}), nunca para as 12 variantes '(Cloistered Cleric)'/'(Warpriest)' alcançáveis só via flag de ator. A nota 6 do mesmo arquivo tem a tabela completa MEDIDA (não a reafirmar): Cloistered — fortitude@3(rank2), spellcasting@7(2)/15(3)/19(4), weapons.simple+unarmed@11(2); Warpriest — fortitude@1(2)+@15(3, não citado no resumo da issue), armor.light+medium@1(1)(+@13(2) condicional a 'feature:divine-defense'), weapons.martial@3(1)/7(2)+weapons.simple+unarmed@7(2), spellcasting@11(2)/19(3) — bate item a item com a tabela simplificada da issue.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/planVM.ts:3911-3927 (chooseClassChoice — adicionar branch `if (slotType === 'doctrine')`, mesmo padrão de `if (slotType === 'bloodline')` já existente)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:~3963 (bloodlineSpellcastingOps — modelo a copiar para uma nova `doctrineProficiencyOps(ctx, featureDoc)`)`; `packages/client/src/lib/sheets/pf2e/planVM.ts (nova tabela hardcoded doctrine→upgrades, mesmo padrão de NON_SPELLCASTER_FOCUS_TRADITION:3055-3057, citando a nota 6 do cleric.json como fonte)`; `tools/importer-pf2e/src/curation/classes/cleric.json (nota 6, já tem os números medidos — fonte de verdade, não precisa remedir)`; `systems/pf2e/src/derivations/build.ts:243-256,280-380 (effectiveRank/stepCharApplyClass — NENHUMA mudança necessária: já lê `entry.system.proficiencyUpgrades` genericamente; basta o array vir populado)`

**Esboço de correção:** Quando o axis 'doctrine' resolve (chooseClassChoice, slotType==='doctrine'), emitir um doc:update no item embutido 'class' do actor que faz merge das 6 linhas doutrina-específicas (tabela da nota 6) em `system.proficiencyUpgrades`, chaveado pelo NOME do featureDoc escolhido ('Cloistered Cleric' vs 'Warpriest' — mesmo nome que já vira o item do slot, ver AXIS_SUFFIX_TO_SLOT_TYPE:2386). Repique (trocar de doutrina) precisa REMOVER as linhas da doutrina anterior antes de aplicar as novas (mesmo cuidado que bloodlineSpellcastingOps:3983 já tem para re-stamp) — a issue avisa explicitamente sobre corromper quem trocou de escolha. Teste NÃO-CIRCULAR: montar um Cleric doc de nível 19 escolhendo 'Warpriest', rodar a derivação real (stepCharApplyClass, systems/pf2e/src/derivations/build.ts) e assert `sys.saves.fortitude.rank===3` (master) e `sys.proficiencies.spellcasting.rank===3` (não 2, que seria o valor incondicional atual) — a asserção vem da regra RAW (tabela do livro, replicada na nota 6 medida contra o vendor), não de reler o próprio proficiencyUpgrades que o código acabou de escrever.

---

## #51 — NUMERO_DESATUALIZADO (esforço G)

**Medição:** Script node varrendo todos os packs (não só os medidos na r22): hoje 723 documentos / 1259 regras em flags.fusion.unconvertedRules (issue dizia 413 docs / 971 regras — pack cresceu com Druid/Monk completo/Kineticist entre r22 e r28). Breakdown por tipo hoje: ItemAlteration 543, AdjustDegreeOfSuccess 123, ChoiceSet 160, AdjustModifier 106, Strike 77, DamageAlteration 51, ActiveEffectLike 41, CriticalSpecialization 30, ActorTraits 32, AdjustStrike 24. CONSUMIDOR: `packages/system-api/src/effects.ts:62-70` declara ItemAlteration/ChoiceSet/ActiveEffectLike/DamageDice/aura/AdjustDegreeOfSuccess como '[V2] reserved — FALLBACK logs and ignores'; nenhuma lógica de derive aplica esses tipos. A ÚNICA exceção real: `planVM.ts` tem uma tabela hand-curated `GRANTED_FEAT_CHOICES` (1 entrada: Basic Concoction) e `choiceSetInventory.ts` classifica ChoiceSets como 'eixo'/'sub-slot'/'pendente' feat-a-feat — cobre uma fração pequena dos 160 ChoiceSet, zero dos outros 8 tipos (ItemAlteration/Strike/AdjustModifier/DamageAlteration/CriticalSpecialization/AdjustStrike/AdjustDegreeOfSuccess/ActorTraits continuam 100% inertes).

**Arquivos:** `packages/system-api/src/effects.ts:18-70`; `packages/client/src/lib/sheets/pf2e/planVM.ts:956-1002 (GRANTED_FEAT_CHOICES)`; `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts`; `packages/shared/src/mechanics.ts:187`

**Esboço de correção:** Construir um consumidor genérico em system-api (derive.ts) que promove ItemAlteration primeiro (o tipo mais frequente, 543 instâncias) de flags.fusion.unconvertedRules para efeito real de derive, começando pelos casos determinísticos (set numeric property). Teste NÃO-CIRCULAR: montar um ator com um item cujo ItemAlteration real (ex. Animal Instinct) altera um dado de dano, e comparar o valor derivado contra a REGRA impressa do PF2e (não contra o próprio unconvertedRules do pack).

**Acoplada a:** #52, #53, #54

---

## #52 — CONFIRMADO (esforço G)

**Medição:** `node -e` sobre class-features-core/mechanics.json: entries['oqbnOYq8BmDDXWid'] (Animal Instinct) = undefined — sem overlay, confirma issue. documents.json: Animal Instinct hoje tem 36 unconvertedRules (ChoiceSet:1, AdjustModifier:2, ItemAlteration:3, Strike:30 — issue dizia 33 Strike, drift pequeno por evolução do pack, não por conserto). choiceSetInventory.ts:160 mostra `class-features-core/Instinct/instinct: "eixo"` (a ESCOLHA de qual instinto já é oferecida no builder desde r22) mas `class-features-core/Animal Instinct/-: "pendente"` (linha 77) — a sub-escolha da forma animal e os 30 Strike continuam sem nenhum consumidor (mesmo grep de #51). Os outros 6 instintos (Superstition, Dragon, Giant, Fury, Elemental, Ligneous) seguem o mesmo padrão 'pendente'.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts:77-207`; `systems/pf2e/packs/class-features-core/documents.json (Animal Instinct, id oqbnOYq8BmDDXWid)`; `systems/pf2e/packs/class-features-core/mechanics.json`

**Esboço de correção:** Depende do consumidor genérico de Strike da #51. Depois disso: oferecer a sub-escolha da forma animal (choiceSetInventory 'Animal Instinct/-' de pendente→sub-slot) e materializar as 30 Strike como ataques desarmados reais. Teste: personagem Bárbaro com Animal Instinct + forma 'Bear' entra em fúria → characterSheetVM expõe uma Strike 'Claw' 1d6 slashing (valor RAW do PF2e para forma Bear, conferido no livro, não no pack).

**Acoplada a:** #51

---

## #53 — CONFIRMADO (esforço G)

**Medição:** 18 documentos 'Bloodline: X' em class-features-core (issue diz '19 linhagens' no título mas a própria #33 já mediu 18 — título desatualizado, corpo da issue não). `node -e` no doc 'Bloodline: Draconic' (_id NGPPWbOwKgGEtC4z): system tem só [actionType,actions,category,description,level,prerequisites,publication,rules,traits] — sem campo estruturado; unconvertedRules = ChoiceSet (10 opções de dragão) + 4 ItemAlteration, nenhum aplicado (mesmo grep de #51: nenhum consumidor de ItemAlteration em runtime). choiceSetInventory.ts:90-94: 'Bloodline/bloodline' = 'eixo' (escolher QUAL linhagem já funciona), mas as sub-escolhas aninhadas (qual dragão, qual elemento, qual gênio) seguem 'pendente'. Recontagem por classe hoje: sorcerer 42/134 docs afetados (31,3%) vs issue 55/158 (34,8%) — proporção parecida, ainda a mais alta junto com Champion.

**Arquivos:** `systems/pf2e/packs/class-features-core/documents.json (18 docs 'Bloodline: *')`; `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts:90-94`; `tools/importer-pf2e/src/curation/classes/sorcerer.json (traditionByBloodline)`

**Esboço de correção:** Mesma dependência da #51 (consumidor de ItemAlteration) + oferecer as 4 sub-escolhas 'pendente' (Draconic/Elemental/Genie/Wyrmblessed) no builder. Teste: Sorcerer Draconic(Fogo) → magia de linhagem concedida deve ter damageType='fire' (comparado contra a tabela RAW de Draconic Exemplars do PF2e), não contra o próprio ItemAlteration do pack.

**Acoplada a:** #51, #33

---

## #54 — CONFIRMADO (esforço M)

**Medição:** `node -e`: doc 'Deity (Champion)' (_id 2zmRjnSK4X2ScHWJ) tem unconvertedRules ChoiceSet:2 + ActorTraits:1 + AdjustStrike:1, nenhum aplicado. Doc 'Cause' (_id G95Xci9mfJ6PTALk) tem ChoiceSet:1, também não aplicado. choiceSetInventory.ts:96 confirma 'Cause/cause'='eixo' (escolher a Causa já funciona) mas nenhum código (`grep 'Retributive Strike\|championReaction'` em planVM.ts) grante a reação de classe correspondente; 'Deity (Champion)/-' e '/deity' seguem 'pendente' (linhas 109-113) — a escolha de deidade nem é oferecida ainda. Recontagem: champion 45/140 docs afetados (32,1%) vs issue 46/143 (32,2%) — praticamente idêntico, segunda classe mais afetada como a issue diz.

**Arquivos:** `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts:96,109-113`; `systems/pf2e/packs/class-features-core/documents.json (Deity (Champion) 2zmRjnSK4X2ScHWJ, Cause G95Xci9mfJ6PTALk)`; `packages/client/src/lib/sheets/pf2e/planVM.ts:993-1002 (padrão GRANTED_FEAT_CHOICES a espelhar)`

**Esboço de correção:** Tabela hand-curated cause→reação (mesmo padrão de GRANTED_FEAT_CHOICES em planVM.ts:993), granting Retributive Strike/Glimpse of Redemption/Liberating Step conforme a Cause escolhida; e oferecer a escolha de Deity (hoje 'pendente'). Teste: Champion causa=Liberator → plano/ficha deve conter a reação 'Liberating Step' (nome RAW do talento no PF2e), verificado no compêndio de actions-core, não inventado.

**Acoplada a:** #51

---

## #55 — CONFIRMADO (esforço G)

**Medição:** Script exato da issue não estava disponível (comentários da issue não trazem reprodutor); aproximação própria (regex 'you (gain|learn)' em descrições com system.rules=[] E unconvertedRules=[] filtrado por trait de classe) encontrou 26 candidatos só no Bard (issue cita 23 como maior classe afetada) — mesma ordem de grandeza, mesmo padrão estrutural confirmado (feats com system.rules vazio cuja prosa concede magia). `grep -r 'Courageous Anthem\|grantsSpell\|SPELL_GRANT'` em packages/ → nenhum resultado: nenhum código trata esse padrão hoje. Decisão de produto continua em aberto (nenhuma menção em specs/17-sistema-pf2e.md).

**Arquivos:** `systems/pf2e/packs/feats-core/documents.json (composições do Bardo: Courageous/Rallying Anthem, Song of Strength, Triple Time etc.)`; `packages/client/src/lib/sheets/pf2e/planVM.ts (local do padrão GRANTED_FEAT_CHOICES a estender)`

**Esboço de correção:** Decisão de produto pendente (escrever à mão vs aceitar manual). Se decidido escrever: seguir o padrão hand-curated de planVM.ts (GRANTED_FEAT_CHOICES), começando pelo Bardo (maior volume). Teste NÃO-CIRCULAR: feat 'Courageous Anthem' concedido → magia de foco 'Courageous Anthem' deve aparecer no repertório do ator, conferido contra o texto RAW do talento (prosa do pack), não contra uma tabela interna nova.

---

## #56 — NUMERO_DESATUALIZADO (esforço G)

**Medição:** Recontagem viva de AdjustDegreeOfSuccess em todos os packs: hoje 123 instâncias em 110 documentos (issue dizia 67 instâncias / 60 documentos — quase dobrou entre r22 e r28 com a entrada de Druid/Kineticist/Monk completo). specs/17-sistema-pf2e.md:432 confirma REQ-PF2-043 ainda [V2]; packages/system-api/src/effects.ts:18-20 confirma AdjustDegreeOfSuccess ainda 'reserved, FALLBACK logs and ignores'. Decisão não foi revisitada desde a issue.

**Arquivos:** `specs/17-sistema-pf2e.md:432 (REQ-PF2-043)`; `packages/system-api/src/effects.ts:18-20`

**Esboço de correção:** Não é conserto de código — é decisão de produto a reabrir com o número atualizado (110 documentos, quase o dobro do que embasou a decisão original). Se promovido a MVP: implementar em system-api/src/derive.ts o pipeline de ajuste de grau de sucesso antes do modificador final ser fixado. Teste: talento 'Evasion' aplicado a um Reflex save que rolaria 'failure' deve resultar em 'success' (regra RAW: sucesso vira crítico e falha vira sucesso), comparado ao texto do talento, não ao próprio unconvertedRules.

---

## #59 — NUMERO_DESATUALIZADO (esforço G)

**Medição:** Rodei o teste real (não reli o .md estático): `cd packages/client && npx vitest run src/lib/sheets/pf2e/__tests__/choice-sets.test.ts` → passou (3/3) e logou `[escolhas] 137 escolha(s) que o builder ainda não oferece` (hoje) vs os '58 pendências de 76 mapeadas' citados na issue (medição de r21, 2026-08-02). `grep -oE '": "(eixo|sub-slot|fora-do-builder|pendente)"' choiceSetInventory.ts | sort | uniq -c` → 13 eixo + 8 fora-do-builder + 2 sub-slot + 137 pendente = 160 ChoiceSets mapeados hoje (vs 76 na issue). O número SUBIU, não caiu — coerente com o Player Core 2 (17 ancestralidades/107 heranças, PR #101 mergeado 2026-08-10, ver memória `project_ancestries_pc2`) ter trazido dezenas de ChoiceSets novos depois da medição original. O mecanismo genérico que a issue pede continua inexistente (grep não encontra nenhuma função de 'sub-slot genérico a partir de ChoiceSet' em planVM.ts).

**Arquivos:** `packages/client/src/lib/sheets/pf2e/choiceSetInventory.ts (160 entradas hoje, 137 pendente)`; `packages/client/src/lib/sheets/pf2e/__tests__/choice-sets.test.ts (mede o número; não reprova por pendências)`; `packages/client/src/lib/sheets/pf2e/planVM.ts (destino do futuro mecanismo genérico — nenhuma função existente hoje)`

**Esboço de correção:** Não há conserto pontual — falta o mecanismo genérico (consumir `item.flags.fusion.unconvertedRules` ChoiceSet do vendor como sub-slot automático) em vez de 137 caminhos escritos à mão. Esboço: nova função em planVM.ts que lê os `choices` do ChoiceSet do item concedente e monta um sub-slot sob o chip, substituindo os casos manuais um a um. Teste NÃO-CIRCULAR possível por amostra: pegar `Fighter/fighterSkill` (Guerreiro treina Acrobática OU Atletismo no nível 1 — regra RAW documentada) e, após o mecanismo genérico, afirmar que o builder oferece exatamente essas duas opções; a asserção vem do livro de regras, não do pack. Efeito esperado e mensurável: mover a entrada de 'pendente' para 'eixo'/'sub-slot' em choiceSetInventory.ts, o que `pendingChoiceSets()` (contagem automática) já capta.

---

## #62 — CONFIRMADO (esforço P)

**Medição:** grep -ri 'unarmed|fist' em packages/client/src, packages/server/src e systems/pf2e/src: nenhum ponto do codigo cria ou concede um item de arma 'Fist' (categoria unarmed) para um personagem novo. systems/pf2e/src/derivations/equipment.ts:127-178 (stepCharCollectEquipment) so RECOLHE de doc.items um weapon com category==='unarmed' que ja exista - nao sintetiza nenhum quando a lista esta vazia. systems/pf2e/packs/*: unica ocorrencia de 'Fist' e em bestiary-core (item de PNJ, formato flat-bonus, nao aplicavel a PC). Powerful Fist (class-features-core/documents.json:33383) so faz upgrade de dado (ItemAlteration damage-dice-faces) sobre um Fist que a regra real do PF2e assume universal, nao concedido por feature. Cobertura de teste: 0 ocorrencias de 'Monk'/'monk' em varredura-classes.test.ts e 0 testes de strikes sem arma equipada em derivations-equipment.test.ts.

**Arquivos:** `systems/pf2e/src/derivations/equipment.ts:127-178 (stepCharCollectEquipment)`; `systems/pf2e/src/__tests__/derivations-equipment.test.ts (novo caso)`

**Esboço de correção:** Em stepCharCollectEquipment, apos varrer doc.items, se nenhum weapon com category==='unarmed' foi encontrado, empurrar em `weapons` um EquippedWeaponInput sintetico de Punho (1d4 contundente, traits ['agile','finesse','nonlethal'], category 'unarmed', runes zeradas) fonte da REGRA do CRB remaster (todo personagem pode atacar desarmado), nunca de um pack. Assercao NAO-CIRCULAR: em derivations-equipment.test.ts, doc com items:[] (sem NENHUM weapon, cenario do Monge) roda stepCharCollectEquipment+stepCharStrikes e espera derived.strikes.length===1 com damageDie 'd4' e damageType 'bludgeoning' - valores escritos a mao no teste a partir da regra do livro, nunca lidos de um pack.

---

## #81 — CONFIRMADO (esforço M)

**Medição:** O defeito descrito na issue ainda existe linha por linha, só deslocado (a issue cita TableScreen.svelte:376/151; hoje é :473-507 e :260-277 — o repo teve commits entre a issue e hoje). Li TableScreen.svelte:473-507: o $effect faz _teardownOrchestrator(); cleanupScene?.(); cleanupScene=null; SINCRONAMENTE no INÍCIO do corpo, e só dentro do void (async()=>{...})() é que cleanupScene = await loadSceneDocument(...) (linha 495) e sceneOrchestrator = _createOrchestrator(...) (496) são atribuídos — exatamente o padrão 'limpa no começo da execução seguinte, não no retorno do efeito' que a issue aponta. onMount:260-277 confirma a segunda pista: cleanupScene = await loadDevScene(canvas) (273) roda ANTES de canvasReady=true (277), então uma chegada de snapshot nesse intervalo é barrada pelo portão canLoadScene (canvasReadyGate.ts:31-33, só checa hasCanvas&&canvasReady, sem contador de geração). Busquei por qualquer guarda de geração/AbortController adicionada desde a issue (grep 'generation|AbortController' em TableScreen.svelte e canvasReadyGate.ts) — não encontrei nenhuma; o único fix que ENTROU no meio tempo foi sceneReloadKey (linha 471), que resolve um bug DIFERENTE (recarregar a cena inteira a cada drag de token) e não toca a race async aqui descrita.

**Arquivos:** `packages/client/src/components/TableScreen.svelte:85 (decl cleanupScene)`; `packages/client/src/components/TableScreen.svelte:103 (decl sceneOrchestrator)`; `packages/client/src/components/TableScreen.svelte:260-277 (onMount — dev-scene load antes do portão)`; `packages/client/src/components/TableScreen.svelte:473-507 ($effect — limpeza no início em vez do retorno)`; `packages/client/src/lib/canvas/canvasReadyGate.ts (ou novo arquivo de máquina de estados de geração)`

**Esboço de correção:** Adicionar um contador de geração (let sceneLoadGeneration=0) ao lado de cleanupScene; capturar const myGeneration=++sceneLoadGeneration ANTES do void(async()=>{...})(); dentro do callback, após cada await (loadSceneDocument e sceneOrchestrator.setup()), checar if(myGeneration!==sceneLoadGeneration) — se mudou, uma carga mais nova já começou: descartar/desfazer o que ESTA carga produziu (teardown do orchestrator recém-criado, chamar a cleanup recém-obtida) e retornar SEM publicar em cleanupScene/sceneOrchestrator. Trocar a limpeza síncrona do topo do $effect por um `return () => { _teardownOrchestrator(); cleanupScene?.(); cleanupScene=null; }` — o retorno do $effect no Svelte 5 roda garantidamente antes da próxima execução E no unmount, que é o pareamento carga↔limpeza que falta hoje. Fazer o onMount's loadDevScene também respeitar o mesmo contador (ou mover a checagem de canvasReady para dentro do $effect de forma que uma troca de geração durante loadDevScene descarte o resultado do dev-scene). Prova NÃO-CIRCULAR: extrair a regra pura 'esta carga ainda é a válida?' para uma função tipo isCurrentGeneration(loadGeneration, currentGeneration) (mesmo padrão de canvasReadyGate.ts) e testar: loadGeneration=1,currentGeneration=2 (uma segunda carga começou antes da primeira resolver) → false; loadGeneration=2,currentGeneration=2 → true. A regra vem do invariante declarado na issue ('a limpeza N nunca deve rodar contra o conteúdo N+1'), não de reler o próprio código depois do fix.

---

## #83 — CONFIRMADO (esforço G)

**Medição:** grep -rn "TokenAddDialog" packages/client/src → 1 arquivo (só o próprio TokenAddDialog.svelte). grep -rn "WallsLayer" packages/client/src → 1 arquivo (só o próprio WallsLayer.ts). Nenhum import, nenhuma instanciação, nenhum <TokenAddDialog/> em lugar nenhum — confirmado também que TableScreen.svelte instancia TokenLayer (linha 529), TileLayer (715) e NoteLayer (727) mas nunca WallsLayer. grep -rn '\$push|\$pull|\$set\b' packages/server/src → 0 arquivos: nenhum operador de coleção existe no servidor. Segui a cadeia de aplicação do diff: doc-handlers.ts:916 chama applyDotPathDiff (linha 1930) que faz output[path]=value literal (sem interpretar $push), e deepMerge (documents/merge.ts:80-87) só entende objeto-vs-objeto (merge) e array (replace) — como o patch {tokens:{$push:{...}}} é um objeto e o alvo é um array, cai no ramo 'else' e o campo tokens inteiro é SUBSTITUÍDO pelo literal {$push:{...}}, que então falha o schema z.array(TokenDocumentSchema) do Scene — bate exatamente com o erro citado na issue ('tokens: Expected array, received object'). Achei também que já existe um caminho CORRETO e testado para criar/apagar documentos embedded (Token, Tile): doc:create/doc:delete com payload.parent={type:'Scene',id}, roteado para handleEmbeddedCreate/handleEmbeddedDelete (doc-handlers.ts:1269-1451, 1636-1738), já usado por TokenInteractionManager.ts:264-292, tokenDrop.ts:71-76 e tileController.ts:44-101 — a issue caracteriza a decisão como 'protocolo pendente' mas o protocolo certo já existe no repo, só não é usado pelos dois arquivos mortos. 'Wall' NÃO está em EMBEDDED_PARENT_MAP (doc-handlers.ts:175-181, só Token/Combatant/Item/Tile/Note) nem tem branch de validação de schema em handleEmbeddedCreate (só Token e Tile são validados contra schema nas linhas 1343-1371; qualquer outro embeddedType cai no 'else' e é aceito SEM validação nenhuma, linha 1416 created.push(raw)) — então rotear Wall pelo caminho embedded exige duas mudanças novas no servidor, não só trocar o payload do cliente.

**Arquivos:** `packages/client/src/components/scenes/TokenAddDialog.svelte:126-163`; `packages/client/src/lib/canvas/walls/WallsLayer.ts:584-622`; `packages/client/src/lib/canvas/walls/WallsLayer.ts:624-645`; `packages/client/src/components/TableScreen.svelte (toolbar/trigger + wiring de WallsLayer perto de TokenLayer:529/TileLayer:715 e _teardownOrchestrator:752)`; `packages/server/src/net/handlers/doc-handlers.ts:65-72 (import WallDocumentSchema)`; `packages/server/src/net/handlers/doc-handlers.ts:175-181 (EMBEDDED_PARENT_MAP += Wall:Scene)`; `packages/server/src/net/handlers/doc-handlers.ts:195 (GM_ONLY_EMBEDDED — decisão)`; `packages/server/src/net/handlers/doc-handlers.ts:1363-1417 (handleEmbeddedCreate — branch de validação Wall)`; `packages/client/src/lib/canvas/__tests__/canvasWiring.test.ts (novas asserções)`

**Esboço de correção:** NÃO inventar $push/$pull no protocolo — o caminho embedded (doc:create/doc:delete com parent:{type:'Scene',id}) já existe, já é testado e já é usado por TokenInteractionManager/tokenDrop/tileController; duplicar como operador de diff criaria uma segunda via de permissão/validação/redação para o mesmo dado. Lado cliente: (1) TokenAddDialog.handleSubmit troca o doc:update com {tokens:{$push:...}} por doc:create {documentType:'Token', data:[{...campos sem _id}], parent:{type:'Scene', id: sceneId}} (mesma forma de tokenDrop.ts:71-76); precisa também de um GESTO real que abra o dialog (hoje não existe nenhum) — botão na TableScreen, marcado SCAFFOLDING se não houver ponto natural. (2) WallsLayer._createWall/_sendWallsDelete trocam walls:{$push:wall}/walls:{$pull:ids} por doc:create/doc:delete com documentType:'Wall' e parent:{type:'Scene',id:sceneId}; WallsLayer precisa ser instanciado/destruído na TableScreen do mesmo jeito que TokenLayer/TileLayer. Lado servidor: adicionar 'Wall':'Scene' em EMBEDDED_PARENT_MAP (senão doc:update embedded de wall — mover parede, trocar estado de porta — falha com 'Unknown parent type: Wall'); adicionar branch WallDocumentSchema.safeParse em handleEmbeddedCreate ao lado de Token/Tile (senão parede embedded entra sem validação nenhuma); decidir se Wall entra em GM_ONLY_EMBEDDED (a issue deixa em aberto — recomendo sim, é geometria de nível como Tile). A redação de segredo de porta (redactSecretDoors/scenePayloadHasSecretDoors) já opera sobre o conteúdo do Scene, então já cobre paredes criadas pelo caminho novo sem trabalho extra — só precisa ser exercitada por teste, não construída. Prova NÃO-CIRCULAR: teste de integração server (padrão embedded-item-actor.test.ts) que manda doc:create Wall autenticado como GM e assere ack.ok + Scene.walls contém a parede com _id GERADO PELO SERVIDOR (não o enviado pelo cliente) — prova que o append de coleção realmente aconteceu, não que o pack ecoa a si mesmo; um segundo teste manda o mesmo payload como PLAYER e assere PERMISSION_DENIED (prova a regra de design, não a re-leitura da tabela de permissão); e extensão de canvasWiring.test.ts com expect(source).toMatch(/new WallsLayer\(/) — reproduz a mutação que a própria issue #83 fez (remover a fiação, rodar a suíte, ver se fica vermelho).

---

## #84 — CONFIRMADO (esforço P)

**Medição:** Li TokenInteractionManager.ts:137: targeting?: TargetingPort; ainda opcional. Li canvasWiring.test.ts inteiro (58 linhas, 5 asserções: TokenInteractionManager constructor, tokenInteraction?.destroy(), attachRuler, disposeRuler?.(), canvas.gridStrategy) — nenhuma delas menciona 'targeting'. Confirmei que a fiação REAL ainda existe hoje: TableScreen.svelte:619-643 constrói `new TokenInteractionManager({... targeting: { isTargetedByMe: ..., toggle: ... } })` — ou seja, o código funciona, só o guarda não cobre esse argumento, exatamente como a issue descreve ('não bloqueia o item: o código funciona hoje'). grep -rn 'new TokenInteractionManager\(' no client → 5 arquivos: TableScreen.svelte + TokenInteractionManager.ts (a própria classe) + 3 arquivos de teste (token-target-gesture.test.ts, token-manager-contract.test.ts, token-config-gesture.test.ts). Chequei token-manager-contract.test.ts: NÃO passa targeting — confirma que se o campo virar obrigatório, esse teste (e possivelmente os outros dois) precisa ser atualizado para passar um stub, o que é esperado mas amplia levemente o raio da correção.

**Arquivos:** `packages/client/src/lib/canvas/tokens/TokenInteractionManager.ts:137`; `packages/client/src/lib/canvas/tokens/__tests__/token-manager-contract.test.ts`; `packages/client/src/lib/canvas/tokens/__tests__/token-target-gesture.test.ts`; `packages/client/src/lib/canvas/tokens/__tests__/token-config-gesture.test.ts`; `packages/client/src/lib/canvas/__tests__/canvasWiring.test.ts`

**Esboço de correção:** Caminho mais barato (o que a própria issue recomenda): tornar `targeting: TargetingPort` obrigatório em TokenInteractionOptions (tirar o `?` da linha 137) — o compilador vira o guarda, e não há mais teste a manter. Isso quebra a compilação dos 3 arquivos de teste que hoje constroem o manager sem targeting; cada um precisa passar um stub mínimo `{ isTargetedByMe: () => false, toggle: async () => {} }`. Alternativa (se algum caller legítimo precisar do campo opcional): adicionar uma 6ª asserção em canvasWiring.test.ts no MESMO formato das outras 5, ex. expect(source).toMatch(/targeting:\s*\{/) ou algo mais específico como /isTargetedByMe:/ — apontando para o call-site real de TableScreen.svelte:640-643. Prova NÃO-CIRCULAR: repetir a MESMA mutação que a issue já fez para achar o problema — remover o bloco targeting:{...} de TableScreen.svelte num diff descartável e confirmar que agora FICA VERMELHO (com a opção 1: `tsc`/typecheck falha; com a opção 2: canvasWiring.test.ts falha) — hoje essa mutação roda 18 arquivos/321 testes todos verdes, então o teste que prova a correção é exatamente o que hoje não pega o defeito.

---

